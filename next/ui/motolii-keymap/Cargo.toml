[package]
name = "motolii-keymap"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# 依存ゼロの leaf crate(motolii-shell-state と同格の設計 — 他のどの pane/engine
# 型も要らない)。`Key`/`Modifiers` は iced の型ではなく**中立表現**として自前で
# 持つ(iced::keyboard::Key へ写せる形にはするが、この crate 自体は iced を
# 引かない)。shell 側の実配線(iced イベント → この crate の中立型への変換)は
# 次切片の仕事(発注書 EXACT TARGET)。
[dependencies]
