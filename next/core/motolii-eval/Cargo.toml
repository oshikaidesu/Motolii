[package]
name = "motolii-eval"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
motolii-core.workspace = true
serde.workspace = true
thiserror.workspace = true

[dev-dependencies]
# 既に workspace 全体で使っている crate(motolii-store 等)を、この crate の
# テストからも使えるようにするだけ — 新規依存の追加ではない。
serde_json.workspace = true

[lints]
workspace = true
