# 裁定ログ(追記のみ)

1裁定1行。取り消し線を引かず、覆した時は新しい行を足して「(YYYY-MM-DD の N を覆す)」と書く。
理由が長い物だけ `../docs/reviews/` に置き、ここには結論を書く。

| # | 日付 | 裁定 |
|---|---|---|
| 1 | 2026-08-20 | ドリフトの累積を1度リセットし、軸を1本にする。旧 workspace は歴史証拠として残す |
| 2 | 2026-08-20 | Document の実体は rerun store。undo は `edit` timeline の時間移動で、自前の undo 機構を作らない |
| 3 | 2026-08-20 | rerun は `crates/store/*` と `re_renderer` だけ引く。viewer 層(egui)は引かない |
| 4 | 2026-08-20 | AE の意味(Layer/Transform/Keyframe/Effect)は `re_types_core` の custom component として Motolii 側に建てる。`re_types` を fork しない |
| 5 | 2026-08-20 | front は iced のみ。pane は store への query の投影で、独自の状態を持たない |
| 6 | 2026-08-20 | 拡張の口は trait 1本。「component を読んで値か画を書く」以外の口を足さない |
| 7 | 2026-08-20 | 規律は `wraps:` / `owns:` marker 1つだけ。リンク台帳・索引・リンク検査を新 workspace に持ち込まない |
| 8 | 2026-08-20 | Document は `comp` 軸に載らない。property track を `edit` 軸へまるごと1行で置き、comp 時間の値は Motolii の評価器が出す(R0-A 実測: `LatestAtQuery` が単一 timeline のみで2次元 query が書けない) |
| 9 | 2026-08-20 | R0 は常設試験として残す。rerun fork の rev を上げた時はこれを回す |
| 10 | 2026-08-20 | 移植は再実装より優先する。`motolii-core`(有理数時刻)と `motolii-eval`(keyframe 補間・bezier 分割)は旧 workspace からそのまま持ってきた。新しく書き直さない |
| 11 | 2026-08-20 | track は `KeyframeTrack` の serde 表現を **1つの component** に入れる。arrow schema へ割らない — 同じ意味の正本が2つになるため。代償は実測で 5.4倍(1000編集×300打点で 3.5MB → 18.8MB)、予算 64MB 内 |
| 12 | 2026-08-20 | 削除は tombstone(`present = false` の append)。`drop_entity_path` を使わない — undo で戻らなくなる |
| 13 | 2026-08-20 | 拡張の trait は**まだ作らない**。2つ目の利用者(compositor)が現れるまで待つ。口を先に決めると、決めた口に合わせて中身が歪む |
| 14 | 2026-08-20 | 合成は `re_renderer` 直叩き。layer = `TexturedRect` の板、重ね順 = `depth_offset`、不透明度 = `multiplicative_tint`、カメラは正射影 `TopLeftCornerAndExtendZ`(world 単位 = ピクセル・原点左上 = AE の comp 座標)。2026-08-11「direct `re_renderer` 禁止」の撤回を実装で確定した |
| 15 | 2026-08-20 | preview も export も `Compositor::render` 1本。同じ入力から byte 一致することを常設試験で縛る(第二経路が生まれたらここが落ちる) |
| 16 | 2026-08-20 | **alpha 付き書き出しは現経路では出せない**。`ScreenshotProcessor` は compositing 後を撮るので alpha が 255 へ潰れる。直し方は `ViewBuilder` の main target を composite 前に読む経路の追加(fork seam は要らない見込み)。限界は試験で固定済みなので、直った日に試験が落ちて気付ける |
| 17 | 2026-08-20 | headless GPU の instance / adapter 選択 / device limits は `re_renderer::device_caps` の物をそのまま使う。自前で limits を書かない(rerun の shader が要求する床とずれた時に原因が追えなくなる) |
| 18 | 2026-08-20 | 1フレームを出す経路は `Engine::render_frame(&StoreView, t, comp)` 1本。preview も export もこれを呼ぶ。「書き出し専用の速い道」を足さない |
| 19 | 2026-08-20 | 素材の種類は `LayerSource` の variant を足すことだけで増やす。動画・静止画・生成物が別経路を持たないようにする |
| 20 | 2026-08-20 | キーを打っていない property は静止値(位置0・不透明度1・大きさは素材のまま)。AE と同じ扱いで、track の有無が意味の有無と一致する |
| 21 | 2026-08-20 | **preview の速さは comp 解像度では買えない。素材側の proxy でしか買えない**。R1 実測(1080p・40枚): 等倍 40ms / comp だけ 1/2 にしても 16〜36ms(ぶれる)/ 素材を 1/4 にすると 5.9ms。律速は fragment の量ではなく **40枚ぶんの素材(332MB)を毎フレーム舐めるバンド幅**で、MSAA を切っても 9% しか変わらなかった。よって proxy(または mipmap)が preview の前提条件であり、「preview 品質」の口は comp ではなく**素材側**に付ける |
| 22 | 2026-08-20 | `motolii-gpu` は新 workspace に建てない。`ctx.rs` は裁定17で上流置換済み、`yuv.rs`(414行)と `transfer.rs`(192行)は `re_renderer` の YUV/readback 経路で置換できる。`resource_ledger.rs` は別の関心事なので必要になった時に単独で `owns:` を主張する |
| 23 | 2026-08-20 | YUV→RGB は **`re_renderer` の `SourceImageDataFormat::Yuv` に載せる**。ffmpeg `-pix_fmt yuv420p` の生バイトが上流の `Y_U_V420` レイアウトとそのまま一致する。自前 WGSL を書かない(色事故の動機ごと上流へ移る) |
| 24 | 2026-08-20 | decode/encode は **ffmpeg サイドカーを維持**する。`re_video` は既にグラフに居るが (a) MP4 のみ (b) 再生指向でフレーム正確ランダムアクセスではない (c) 全バイトをメモリに載せる (d) encode/mux を持たない — 書き出しの契約と噛み合わない。この4点が変わったら再裁定する |
| 25 | 2026-08-20 | 合否の定義は `GOALS.md` 1枚。ここに無い物を「足りない」と数えない(除外リストを含む) |
| 26 | 2026-08-20 | Stage は **iced の `shader::Primitive` 経由で iced の device の上に re_renderer を建てる**(= 本当の埋め込み)。CPU 読み戻しは export と試験だけに使う。根拠: 一次資料(pin 済み fork `wgpu/src/primitive.rs:14-64`)で `prepare(.., device: &wgpu::Device, queue: &wgpu::Queue, ..)` と `render(.., encoder: &mut CommandEncoder, target: &TextureView, ..)` が生の wgpu を渡すことを確認 — iced の拡張点は **toolkit の層ではなく wgpu の層**にある。読み戻しは 1080p で 1.7ms かかる |
| 27 | 2026-08-20 | ~~別 device でも byte 一致するので preview(iced の device)と export(headless)が別 device でも背骨2は崩れない~~ → **主張が過大だった**(同日の敵対的レビュー)。実測したのは `Compositor::headless()` を2回呼んだだけで、**iced の device は1度も通していない**。言えるのは「同じ adapter・同じ limits・同じ RenderConfig の device 2個なら一致する」まで。iced の device を跨ぐ一致は**未検証**で、裁定26 を実装する時に測る |
| 28 | 2026-08-20 | `LayerSource::Media` は **動画も静止画も同じ variant**。素材種で経路を分けない(分けると片方だけ直る欠陥が生まれる — 初回タッチ観察の再発) |
| 29 | 2026-08-20 | 素材の大きさは Document が持たない。probe が決め、engine が「track も declared も無い軸」だけを実寸で埋める。AE の「キーを打っていない property は静止値」の延長 |
| 30 | 2026-08-20 | 旧 `motolii-export`(913行)は移植しない。大半が graph / plugin 機構で、評価経路が1本になった今は要らない。移すのは**機構ではなく意味**(報告=現物 / 中断で残骸なし / 音声は後段 mux)。新 export は `Engine::render_frame` を回すだけの薄い口にする |
| 31 | 2026-08-20 | engine が抱えるフレーム texture は**上限つき**(8枚)。3〜5分の MV は 5,400〜9,000フレームあり、溜め込むと約27GBでメモリが死ぬ。順次走査で要るのは直近の数枚だけ。試験 `long_export_does_not_accumulate_frames` が上限を守らせる |
| 32 | 2026-08-20 | 時刻⇄フレームの写像は `motolii-core` の正準口のみ。柵は**パターン列挙をやめ**、「fps と f64/f32 が同じ式に出てくること」自体を禁じる(`tm4_no_float_frame_math.rs`)。旧の5文字列 grep は engine の f64 換算を素通りさせた |
| 33 | 2026-08-20 | `CompSpec` は `motolii-core` に置く。`motolii-export` が `motolii-compositor` を引くと第二の合成器を建てられるので、**型を出して依存を切る**。柵は `motolii-export/tests/fence.rs` が依存グラフを見る |
| 34 | 2026-08-20 | `motolii-store` の marker は `owns:`。`wraps:` を名乗った crate の中に `owns:` の中身(`fingerprint.rs` / `resolve`)が入ると、crate の根しか見ない `check.sh` が空振りする |
| 35 | 2026-08-20 | `PropertyId` は layer 自身の component 名(`meta` / `present`)を予約語として弾く。弾かないと `PropertyId::new("meta")` が素材と重ね順を上書きする |
| 36 | 2026-08-20 | 重ね順の型は上流の `re_renderer::DepthOffset`(`i16`)に合わせる。`i32` のまま渡すと 32768 以上で符号が反転し、CPU の並べ替えと GPU の前後関係が食い違う |
| 37 | 2026-08-20 | 読み口は「無い」と「読めない」を区別する(`Result<Option<T>>`)。同義にすると壊れた Document が静かに既定値へ落ち、利用者には「値が勝手に戻った」としか見えない(M13) |
| 38 | 2026-08-20 | 素材の外の時刻はその layer だけ描かない(フレーム全体を落とさない)。`nb_frames` を見る。M4 と M16 の両方に効く |
| 39 | 2026-08-20 | **スクラッチは最低限・保守はしたくない**(利用者裁定)。移植したが `next/` から未参照のコードは落とす — 抱えると保守対象になり、`check.sh` が `owns:` の重さとして数えて自前実装の量を偽る。要る日に旧 workspace から持ってくればよい。第1弾: `motolii-core` の camera / canonical / time_map / quality = 1,256行 |
| 40 | 2026-08-20 | comp の設定(解像度・fps・尺)は **Document が持つ**(`Composition`)。以前は `render_frame(view, t, comp)` と `ExportJob { comp, fps }` が別々に持ち、**preview と export が違う入力を渡せた**。上流の `set_recording_property` は `TimePoint::STATIC` で undo が効かないので、layer と同じ `edit` timeline 上の普通の entity として置く(新しい機構を足さない) |
| 41 | 2026-08-20 | 置き方は `motolii-core::LayerPlacement` を store と合成器で**共有**する。並べ直すと property を1つ足すたびに6箇所を触ることになり、旧 `inspector_model.rs` が3世代になった構造の1世代目になる |
| 42 | 2026-08-20 | 変化検出は `Document::revision()`(store 世代 + edit 位置)。`EntityDb::generation` だけでは undo/redo を捉えられず、front が `last_edit_head` を自分で持つ入口になる |
| 43 | 2026-08-20 | **「保守をしたくない」を軸に格上げ**(利用者裁定)。自前で持つコードは資産ではなく負債で、薄いラッパーであることはその負債を最小にするための手段にすぎない。禁じるのは6つ: 上流にある物を書かない / 使われていない物を置かない / 使う分だけ移植する / 抽象を先に作らない / 器具と台帳を増やさない / 「一時的に」を作らない。`check.sh` が毎回 `owns:` の総行数を出し、**それは下がるべき数字**として扱う |
| 44 | 2026-08-20 | **裁定26 を撤回**。Stage は iced の device に re_renderer を建てるのではなく、**CPU 経路**(合成結果の RGBA を `image` widget へ渡す)にする。軸4「保守をしたくない」で計算が変わった: GPU 共有は (a) iced が `Primitive::prepare` に adapter を渡さないので instance を作り直して adapter を選び直す迂回が要る (b) `max_bind_groups` の床という fork seam を1本抱え続ける。CPU 経路の代償は読み戻し 1.7ms/1080p(preview 解像度ならその 1/4)で、**fork を1本減らせる**方を採る |
| 45 | 2026-08-20 | **iced は上流をそのまま引く**(fork しない)。裁定44 で seam 2 が要らなくなり、seam 1(web-sys の完全一致解除)も 0.14 では解決に出てこなかった。fork が1本減った |
| 46 | 2026-08-20 | front が持ってよい状態は `Session`(選択と再生位置)だけ。Document の写しは持たない。選択と再生位置は undo の対象ではない(rerun も選択は blueprint store の外)。**1箇所で持ち全 pane がそこを読む**ので M14 は満たされる |
| 47 | 2026-08-20 | `Document::mark_undo_floor()` — 起動直後や project を開いた直後の状態は編集ではないので戻せてはいけない。呼ばないと利用者が既定値を undo で消し、**Stage が理由もなく空になる**(実際に起きた) |
| 48 | 2026-08-20 | **1操作 = 1 undo** は `Document::apply_all`(複数 intent を1つの edit 刻みへ)で満たす。ドラッグは途中経過を pane が持ち確定の1件だけが intent なので元から1 undo — 畳む口が要るのは「本質的に複数 intent な1操作」だけ |
| 49 | 2026-08-20 | 落下は Message にして受ける(窓の event を直に処理しない)。運転席が窓を開けずに同じ道を通せる。winit は1ファイル1事象なので**描画要求を区切り**にして溜めた分をまとめて1操作にする |
| 50 | 2026-08-20 | **バックの完成を先に**(利用者裁定)。iced を採ったのは「バックができていれば UI は後から生えてくる」ため。よって UI の見栄えより、Document の意味の穴を先に塞ぐ |
| 51 | 2026-08-20 | layer は `LayerTiming { start, duration, source_in }` を持つ。**move / trim / split / 速度はすべてこの型の上に乗り、intent は `SetTiming` 1つ**。専用 intent を操作ごとに足さない |
| 52 | 2026-08-20 | 置いた時の尺 = **min(素材の尺, comp の残り)** は `LayerTiming::place` が持つ。**shell に書かせない** — 書かせると面ごとに違う置き方が生まれる(旧 workspace の import_seat と browser で起きた形) |
| 53 | 2026-08-20 | comp 時刻 → 素材フレームの写像は **Document が持つ**(`resolve` が `source_frame` まで解決して返す)。engine は時間の計算をしない — engine が別の写像を持つと時刻の正本が2本になる(2026-08-20 に一度やった失敗) |
| 54 | 2026-08-20 | **保存形式を決める時に Lottie のスキーマを参照する**(利用者の着想。調査プロジェクトは立てない)。Lottie は JSON で仕様が公開されており、comp / layer / transform / bezier イージング付き keyframe / mask / effect を持つ = Motolii の模型に近い。**自前スキーマを発明しないための一次資料**として、保存形式を決める瞬間に読む。AE の `.aep` は非公開バイナリで保存形式の候補ではない(将来の import 経路として別問題)。Rust 側の先例に `velato`(Vello 用 Lottie レンダラ)と `lottie-rs` がある |
| 55 | 2026-08-20 | **保存形式は上流の `.rrd` をそのまま使う**(自前形式を発明しない)。store の中身がそのまま file になる。**危険**: fork の rev を上げると古い project が読めなくなりうるので、rev を上げる時は往復試験を必ず回す |
| 56 | 2026-08-20 | **保存する時は履歴を畳む**。store は全 edit 刻みを持つので、そのまま書くと project file が編集回数に比例して伸びる(R0-1 実測で 1000編集×300打点 = 18.8MB)。畳んだ実測は 204編集のあと 30KB。セッションを跨いだ undo は捨てる — 普通の編集ソフトと同じ |
| 57 | 2026-08-20 | property の一覧は **store に聞く**(`all_components_for_entity`)。Document 側に一覧を別に持つと、実体とずれた台帳がもう1つ生まれる。Inspector が行を並べる時も同じ口を使う |
| 58 | 2026-08-20 | **transform の意味は Lottie から取る**(発明しない)。公式仕様 + velato + zimond/lottie-rs の3つで裏を取った。構成 = `anchor` / `position` / `scale` / `rotation`(度・時計回り)/ `skew`+`skew_axis`。**適用順序**は `translate(p) · rotate(r) · skew · scale(s) · translate(-a)`(Rust 実装2つで一致)。単位は Lottie のパーセントを採らず 1.0 基準、rotation は度のまま(人が読める) |
| 59 | 2026-08-20 | **`size.width` / `size.height` を property から外す**。Lottie に「レイヤの寸法」property は無い — 寸法は素材固有で、大きさは `scale` でしか動かない。両方持つと**同じことを言う正本が2つ**になる。`size` は UI の便宜(入力すると scale に落ちる)へ格下げ |
| 60 | 2026-08-20 | **`top_left` を捨てて `position` にする**。`top_left ≈ position` が成り立つのは anchor=0・scale=1・rotation=0 の間だけで、回転を入れた瞬間「左上角」は保存値として意味を失う。position は **anchor が着地する点** |
| 61 | 2026-08-20 | **position は Vec2 の単一 property**(x/y の別 track にしない)。別 track だと (a) 空間ベジェ `ti`/`to`(曲線モーションパス)を将来足せない (b) auto-orient が定義できない (c) x と y でキー時刻がズレた不正状態が作れる。split は Lottie と同じく**variant として後で**持つ |
| 62 | 2026-08-20 | **`opacity` は transform の外**(現状維持)。Lottie は形式上 `ks` の中に置くが行列に合成せず親から継承もしない。velato も剥がして返す。**継承しないものを継承する箱に入れない** |
| 63 | 2026-08-20 | `LayerTiming.source_in` は維持し Lottie の `st` に寄せない(代数的に等価: `st = start - source_in`)。編集者には「素材の何フレーム目から」の方が直接的で、trim が1フィールドの加算になる。**ただし `sr`(速度)の穴を今のうちに空ける** — 今の `source_in + (comp - start)` は 1:1 固定しか表せない。Lottie の `(f - st)/sr` が構造的に速度を持てる形になっているのが、この比較で得た一番の教訓 |
| 64 | 2026-08-20 | **Lottie に寄せてはいけない箇所**(Motolii が既に正しい): `fps` は有理数(Lottie の `fr: f64` は 30000/1001 を表せない)/ keyframe の `t` は `RationalTime`(float フレーム番号より精度が高い)/ `duration` を持つ(`op` と違い不正状態が作れない)/ `ip≠0` の作業領域は view の関心であって document の関心ではない |
| 65 | 2026-08-20 | **採らない Lottie 由来のもの**: パーセント単位 / `animated` フラグ(値かキー列かは enum で表す)/ `e`(deprecated・正本が2つになる)/ `ind` によるレイヤ同一性(挿入削除で壊れる。`LayerId` がある)/ 3D 系 / `tm` を timing に混ぜる(property であって timing ではない) |
| 66 | 2026-08-20 | **matte の `tt`/`td`/`tp` 3フィールド構造は採らない**。「される側」と「する側」の二重帳簿で片方だけ書ける不正状態が作れる。「このレイヤは、あのレイヤ(`LayerId`)を、このモードでマットにする」の1フィールドに畳む。**`tp` 省略時に「1つ上のレイヤ」を暗黙参照する規則は絶対に採らない** — 編集ソフトで**並べ替えが合成結果を黙って変える**のは致命的。MatteMode の4値(Alpha/InvertedAlpha/Luma/InvertedLuma)は AE の語彙なので採る |
| 67 | 2026-08-20 | blend mode の enum は Lottie の 0..15 を採る(AE / Photoshop / peniko / wgpu で共通の語彙で発明の余地が無い)。`Add` / `HardMix` は velato も落としているので後回し |
| 68 | 2026-08-20 | **「作る瞬間に読む」をやめ、先に地図を作って機械で照合する**(利用者裁定)。作る瞬間に読む方式は、**読まなかった物が構造的に見えない**。上流のスキーマ(`reference/lottie.schema.json`、上流そのまま)から全語彙 656項目を機械生成し、1項目ずつ状態(採用済/採用予定/不採用/未判定)を書く表を置く。試験 `lottie_coverage.rs` が「スキーマにあって表に無い」「表にあってスキーマに無い」を落とす。**未判定の数 = AE の意味のうちまだ向き合っていない量**として `check.sh` が毎回出す。初回 456/486 = 94% が未判定 |
| 69 | 2026-08-20 | transform を実装。`LayerPlacement` は `glam::Affine2` を持ち、行列を組むのは `LayerPlacement::from_transform` **1箇所だけ**(適用順序の正本)。合成器は変換後の基底ベクトルを `TexturedRect` の `extent_u`/`extent_v` へ渡すので、**回転も拡大も skew もシェーダを1行も変えずに通る** |
| 70 | 2026-08-20 | **effect は Document が型で持たない**(並列調査の結論)。Document が持つのは「どの effect(**plugin id 文字列**)を・どの順で・どの param で・有効か」だけ。決め手は D6「拡張の口が trait 1本。first/third-party が同じ口」— Document に `GaussianBlur` 型がある日に D6 は嘘になる(first-party だけ特権経路を持つ)。加えて軸4: effect を型で持つと `owns:` が effect の数だけ増える。gaussian-blur / drop-shadow は **first-party plugin として同梱**するが型にはしない |
| 71 | 2026-08-20 | **レイヤスタイル(styles 78件)は全部不採用**。effect の第二機構で、`drop-shadow-style` の5 param が `drop-shadow-effect` と1対1で重なる。両方持つと同じ画の正本が2つ(裁定59 の形)。effect stack 1本に畳む |
| 72 | 2026-08-20 | effect の param は **名前つき map**(位置配列にしない)。上流スキーマ自身が param を `'00'`/`'01'` としか呼べていない(stroke で11本中10本、pro-levels で6本)= 位置 identity 破綻の現物。param の型語彙は `effect-values` から取る: scalar / **Bool** / **Enum** / color / Vec2 / LayerId。**全 param が animatable** なので既存の `KeyframeTrack` + `PropertyId` にそのまま乗る = **新機構ゼロ** |
| 73 | 2026-08-20 | **shape layer は持つ**。ただし持つのは「1つのパス源 + 順序付き演算子スタック + fill/stroke 各1」で、Lottie の `group.it`(兄弟順スコープ)は**採らない** — 修飾子が「同じ配列の手前の兄弟」に暗黙に効く模型は裁定66 と同型で、並べ替えが結果を黙って変える。根拠は `docs/concept.md:204`(2026-07-10、利用者明言「絶対に必要」)がパス演算子ファミリーを v1 コア要件としており、旧 `pathgeom.rs` に trim / repeater / offset / zigzag / pucker-bloat / round-corners / twist / wiggle が**実装済み**なこと |
| 74 | 2026-08-20 | パスブーリアン(merge-paths)は恒久不採用(`concept.md` 既決)。**中マドは fill-rule(nonzero/evenodd)が引き受ける**。primitive ごとの位置・回転・角丸も落とす — 層の transform と repeater の anchor で賄え、持つと正本が2つになる |
| 75 | 2026-08-20 | **テキストは AE のアニメータ模型(animator + range selector)を採る**。`text-model.md` の未決3つが Lottie の語彙で埋まる: grouping alignment(§6)/ selector shape 6値(§6)/ based-on の「空白を除く文字」(単語分割器を回避する安い代替)。ただし **selector には seed を必ず持たせる** — Lottie の `rn` は seed が無く再生器ごとに結果が変わり、Preview=Export(M15/D1)と両立しない |
| 76 | 2026-08-20 | **`text-model.md` §1 の「アニメーターは変形と不透明度のみ・リフローしない」は事実と食い違う**。AE/Lottie のアニメーターは**送り幅(`t`)と行送り(`ls`)を動かす**。二層分離では足りず「再シェーピングする / 位置を再累積する / 変形だけ」の**3層**が要る。凍結の前提としてこの表を書き換える |
| 77 | 2026-08-20 | **`style_spans`(文字範囲ごとのフォント/サイズ)は Lottie から凍結できない**。Lottie の text-document は `f`/`s` が document 単位のスカラ1個で、範囲スタイルの語彙が存在しない。v1 に入れるなら別の先例(CSS inline / DirectWrite text range / Rive text modifier)を引く回が1つ要る。落とすなら v1 要件から外して `scale` アニメーターで代用と明記する。**テキスト凍結の最大の分岐** |
| 78 | 2026-08-20 | `Value` の型体系を地図が正した。**足りない型2つ**: `Value::Path`(マスク形状。v/i/o の長さ一致を型で保証する必要があり `List` の入れ子では表せない)と `Value::Bool`(mask.inv / bezier.c / auto-orient。今は `F64(0.0/1.0)` で、**批判した `int-boolean` と全く同じ形**)。**要らない型3つ**(`next/` で参照ゼロ): `Vec3`(3成分は全部 3D 系で裁定65)/ `AssetRef`(素材参照は `LayerMeta` 側で property ではない)/ `List`(載せる物が無い)。**列挙は `Value` に足さない** — キーを打たない静止設定なので `LayerMeta` 側の component。入れると「補間できない `Value`」が増える |
| 79 | 2026-08-20 | Lottie の「表 + refId」(assets / fonts / chars / 各 layer の refId)は**1枚 JSON を自己完結させる配信の都合**で、編集器の Document の意味ではない。素材は layer が直に持ち(`LayerSource::Media`)、同一性は fingerprint が持つ。**これ1本で 20件以上が落ちた** |
| 80 | 2026-08-20 | **「採用済」を自己申告にしない**。地図の `evidence` 列に**コード中に実在する識別子**を書き、試験 `adopted_rows_point_at_real_code` が `next/` 全体を grep して確かめる。書くだけで地図が埋まると、地図が嘘をつく |
| 81 | 2026-08-20 | **発注単位は地図の `unit` 列**。別の台帳(backlog / roadmap)を作らない — 束は地図の見え方の1つにすぎない(軸4「台帳を増やさない」)。**完了条件 = その束の行が全部 採用済 + evidence 実在**なので機械で判定できる。12束: text 44 / shape-1 38 / shape-2 27 / layer-meta 16 / mask 11 / effect 10 / slot 4 / split-position 3 / shape-3 3 / marker 3 / transform-skew 2 / motion-path 2 |
| 82 | 2026-08-20 | **`style_spans`(1つのテキストレイヤー内で文字範囲ごとにフォント・サイズ・色を変える)は v1 必須**(利用者裁定)。**「文字MV」ならなおさら**。`scale` アニメーターでの代用は成立しない — アニメーターは変形だけで**組版が動かない**ので、拡大しても周りの文字が詰まらず隙間が不自然になる(`text-model.md:62-64` が既にこの区別を書いている)。裁定77 の分岐はこちらで決着 |
| 83 | 2026-08-20 | **テキストは「あると強い」ではなく製品の芯**(利用者の「文字MV」発言)。GOALS の分類を標準 S22 から引き上げる。ただし合否条件としての観測可能な形はこれから書く |
| 84 | 2026-08-20 | 範囲スタイルは **Lottie から凍結できない**ので、別の先例を1回引く(CSS inline / DirectWrite `IDWriteTextLayout` / Rive text / AttributedString / Flutter TextSpan)。最大の分岐は **flat span list か木か** — 裁定66/73 が「修飾子が兄弟に暗黙に効く模型は並べ替えが結果を黙って変えるので採らない」と既に決めており、同じ論理が効く見込み |
| 85 | 2026-08-20 | **`style_spans` は「スタイル表 + 本文の連続分割」の2枚で持つ**。`styles: [Style]`(同一性を持つ表)+ `runs: [(len, style_index)]`(`content` を隙間なく重なりなく覆う分割)。Rive(`TextValueRun.styleId → TextStyle`)と parley(`push_style() -> u16` + `push_style_run(index, range)`)が独立に到達した形で、Swift `AttributedString` の内部表現も同型。**スパンに値を直書きしない**のは、`style_spans[2].font_size` のような添字つき名前だと2文字消しただけで track が別のスパンに付くから — 裁定65(`ind` によるレイヤ同一性を捨てた)と**完全に同型**の問題 |
| 86 | 2026-08-20 | 木を採らない。ただし**理由は裁定66 とは別**である点に注意。木に暗黙隣接参照は無い(親子は明示)。木の欠陥は「**選択して属性を付ける操作が構造を黙って書き換える**」ことで、HTML が adoption agency algorithm を持つ理由がこれ。病気の科は同じだが症状が違う。加えて **木を採る先例も shaping 直前で必ず flat に潰している**(parley の Tree/Ranged 両ビルダは同じ出力を作る)= flat run list は組版の入力形式として普遍で、木は markup を書くための入力糖衣 |
| 87 | 2026-08-20 | **範囲は絶対オフセットではなく長さ**で持つ(本文編集で以降が自動的にずれる)。**アンカーを採る先例は6つ中ゼロ**。AE は明確な反面事例 — 絶対オフセットで、長さが変わると例外を投げ、しかも「**長さが戻ると再び有効になりうる**」= 同じ範囲が違う文字に黙って付き直す(裁定37「無いと読めないを区別する」の逆) |
| 88 | 2026-08-20 | **重なりは編集の動詞であって保存形ではない**。保存は非重複の分割。「太字を選択範囲に適用」は `apply(property, range)` という intent で、適用時に property 単位で後勝ち解決する。全先例(DirectWrite / Swift / parley)が入力の重なりを**その場で正規化して消している** |
| 89 | 2026-08-20 | **隣接同値ランの併合は最適化ではなく正しさの契約**。スパン境界は itemize を割るので、併合しないと合字・カーニングがそこで切れ、**意味が同じ2つの Document が違う画を出す**。D1(Preview=Export)の隣の性質として `text-model.md` §3 に明文化する |
| 90 | 2026-08-20 | **`text-model.md` §2.1 は循環している**。「スパン境界の単位はクラスタ(P6 クラスタ対応表の単位)」だが、**P6 のクラスタ対応表は shaping の出力**で、**スパン境界は shaping の入力**。分ける: セレクタ = shaping クラスタ(現行どおり正しい)/ **スパン境界 = `content` 上の UAX#29 拡張書記素クラスタ**(shaping 前に計算でき循環しない) |
| 91 | 2026-08-20 | `size_ratio` を捨てて**絶対サイズ**にする。比率を採る先例はゼロで、基底サイズと二重帳簿になる(裁定59 の形) |
| 92 | 2026-08-20 | **v1 でスパン style はキーフレーム化しない**。動く組版はセレクタつきの組版アニメーター(裁定76 の第2層)が引き受ける。**Rive とは明示的に分かれる** — Rive は `TextStyle.fontSize` 等を `animates: true` にして毎フレーム再シェープするが、Rive は1 Text に run が数個の世界で、Motolii は歌詞1行を数十クラスタで扱う。表 + 添字にしてあるので、後日 `text.style.{id}.{attr}` という**平坦な `PropertyId`** で乗せられる(スキーマ変更にならない) |
| 93 | 2026-08-20 | v1 の範囲属性は **font / size(絶対)/ fill / tracking / line-height**(全先例の共通部分)+ **縁取り3つ(color / width / over-fill)**。縁取りは共通部分に無く、持っているのは **AE と Rive = モーショングラフィックの先例だけ**なので、歌詞用途の裏が2件で取れている。**可変フォント軸も入れる**(Rive `TextStyleAxis` が `animates: true, bindable: true` を持ち、P6 の「軸だけはスタイル層でアニメ可」の例外に先例がつく)。**baseline-shift は入れない**(Rive が持たず、position で大半が代替できる) |
| 94 | 2026-08-20 | 末尾・境界に打たれた文字は**直前のランを継ぐ**(Swift `inheritedByAddedText` の既定と同じ)。これが無いと「打った文字だけ既定フォントに戻る」という AE でよくある不満形が再現する。Swift の属性ごと宣言機構(`runBoundaries` / `invalidationConditions`)は v1 では採らない(軸4)が、**この既定1本だけは要る** |
| 95 | 2026-08-20 | **Rive の `dev/defs/text/*.json`(20クラス61 property・MIT)を Lottie 地図と同じ TSV に `source` 列付きで足す**。別ファイルにすると台帳が2つになる(裁定81)。Rive の defs は Lottie schema が持たない **`animates` / `bindable` / `runtime` を property ごとに持つ**ので、「範囲スタイルはキーフレーム化できるか」に機械で答えられる |
| 96 | 2026-08-20 | `Value` を地図の結論どおり是正した。`Bool` と `Path` を足し、`Vec3` / `AssetRef` / `List` を落とした。**`Bool` の補間は Hold** — 0.5 で 0.5 になる型で真偽を持っていたのが元の欠陥。パスの補間は**頂点数と開閉が一致する時だけ**(AE も揃えないと補間しない)。`PathVertex` を1つの型にしたのは、`v`/`i`/`o` を別々の可変長配列にすると長さが食い違う不正状態が作れるから |
| 97 | 2026-08-20 | **Rive の text defs 73行を地図へ**(裁定95 の実施)。`source` 列を足して `lottie` / `rive` を分け、スキーマとの機械照合は `source=lottie` の行だけを見る。実利は `runtime: false` の6行が「**これは Document ではなく編集器の都合**」を**機械可読で申告している**こと — Lottie schema にこの区別は無く、`fFamily` と `fPath` のどちらが正本かを人が推論するしかなかった判断が1フラグで済んだ |
| 98 | 2026-08-20 | **地図が黙っていた穴を1つ塞ぐ**: line-height と tracking の正本が3つになっていた(document 基底 / スパン / アニメーターの加算)。**Rive には document 基底が存在しない** — `Text` はフォントもサイズも持たず全部 `TextStyle` にある。裁定85 が「表 + 分割が content を隙間なく覆う」と決めた以上、**Lottie の `text-document f/s/fc/lh/tr/sc/sw/of` は独立した document 値ではなく「スタイル表の既定行(index 0)」と読み直す**。そうしないとフォントを設定する場所が2つできる |
| 99 | 2026-08-20 | **OpenType feature の置き場を作る**(`text-style-feature.tag` / `featureValue` を採用予定)。地図の `text-document ca`(Text Caps)不採用は「Small Caps は OpenType feature が正本」と書いたのに、**その正本の置き場が地図に無かった**。日本語歌詞は `palt`(プロポーショナルメトリクス)の有無で詰め方が別物になるので実用上も効く。Lottie に語彙が無く、`text-model.md` §5 は P6 契約に入れただけで誰が値を持つかを書いていない |
| 100 | 2026-08-20 | `text-style` という名前が Lottie と Rive で**正反対の物**を指す(Lottie = アニメーターが動かす property の束 / Rive = スパンの組版スタイル)。主キーが衝突しないので機械は落ちないが、読む人が取り違える。`source` 列だけが区別している状態であることを記録しておく |
| 101 | 2026-08-20 | **「seed 付き randomize」には先例が1つも無い**(Lottie の `rn` は seed 無し、Rive にはランダム化自体が無い)。裁定75 の「seed を必ず持つ」は Preview=Export(M15/D1)から導いた Motolii 独自の要求であって、先例の裏は取れていない。実装時に自前で設計する必要がある |
| 102 | 2026-08-20 | **試験は debug でも回す**。`--release` は `debug_assert!` を消すので、上流(rerun)が仕込んだ表明が全部黙る。実際に `Document::load` の store 同一性のずれを**release では見逃していた**(shape-1 レーンが debug で踏んで発覚)。速度のために release を常用するのは構わないが、**取り込みの前に一度は debug を通す** |
| 103 | 2026-08-20 | **store の同一性は file が持つ**。`Document::load` は `Document::new()`(= 新しい乱数 `StoreId`)を作ってから file のメッセージを流し込んでいたので、store 自身の id とメッセージの id が食い違っていた。upstream は `debug_assert` で叩くだけなので release では黙って通り、**保存物と store の同一性がずれたまま動く**。最初のメッセージの `store_id` で store を建てるよう修正。R0-4 probe も同じ誤りだったので揃えた |
| 104 | 2026-08-20 | **`shape-1` 束が完了**(38/38、evidence 全件がコードに実在)。新 crate `engine/motolii-vector`、公開口は `render(&Shape, &Canvas) -> Raster` **1本だけ**。ラスタライザは `tiny-skia`(既に `iced_tiny_skia` 経由で lock に居るので**依存が1本も増えない**)。移植648行 / 新規536行 — 旧 `pathgeom.rs` 1,442行のうち 45% だけを取り、残りは `shape-2` の日に取る |
| 105 | 2026-08-20 | shape-1 が明かした設計の穴3つ(記録のみ、今は直さない): (a) **ラスタライズ解像度の正本がどこにも無い** — ベクタは「最終デバイス解像度で焼く」のが本質なので、層の実効スケールを知っている engine が `Canvas` を決めるべきだが未決。裁定21「preview は素材側 proxy でしか速くならない」の隣で **shape だけは解像度を後から選べる素材**という非対称がある (b) **演算子は「純粋にパス→パス」ではなくなった** — `so`/`eo`(コピーごとの不透明度)の捨て先が無くなり、スタックは `Vec<(Path, weight)>` を運ぶ (c) `PathSource::Bezier` は輪郭の**列**を持つ — `group.it` を落とした代償の受け皿で、「1つのパス源」は正確には「1つの輪郭の集合」 |
| 106 | 2026-08-20 | **Rive の defs も固定 revision で vendor する**(`reference/rive-text-defs/`、`PINNED_REVISION` に sha)。調査結果だけを表へ写すと上流が動いた時に黙ってずれる — 実際、同じ調査の1回目(61 property)と2回目(65)で差が出た。試験 `the_map_covers_the_rive_defs` が Lottie と同じ2方向照合をする |
| 107 | 2026-08-20 | **引き継ぎ§8の「選択と playhead の置き場」は未決ではない — 裁定46で決定・実装済み**(`motolii-shell` の `Session`)。調査で裏付けを追加: (a) rerun 上流は blueprint という第二 store を実在させながら、選択(`ApplicationSelectionState`)と playhead(`TimeControl`)を**どちらの store にも置かず** `AppState` の plain struct で持つ(commit `954bf95a4`)。第二 store 案は先人自身が採らなかった形 (b) 裁定8(`LatestAtQuery` は単一 timeline)が「同一 store 内の別軸」案を機構面で塞ぐ。scrub/選択で store 世代が動くと `revision`(裁定42)の意味も壊れる (c) `Session` は store 発行済みの `LayerId` とフレーム番号しか持たず Document の意味を再解釈しないので、裁定5が警戒する翻訳層に当たらない。**将来「前回カーソル位置の復元」が要件化した日も、`Document::apply` / `edit` timeline は経由させず、明示的な非 undo 書き込み経路を個別に用意する**(rerun `TimeControl` の先例)。今は実装しない(裁定13と同じ理由) |
| 108 | 2026-08-20 | **mask 束の着地で出た訂正と穴**(レーン報告、supervisor 検分済み): (a) **裁定78 の用例訂正** — `mask.inv` は Lottie/AE とも静止 boolean でトラックにならない。`Value::Bool` の実用例は effect param 側(裁定72) (b) `persist.rs` の `flattened()` が保存対象を手で列挙しており、**component を1つ足すと保存から黙って消える**。裁定57「store に聞く」に反する構造で、mask ぶんは往復試験で縛ったが根は残る — **layer-meta 束で構造修正する** (c) `Intent::SetMeta` が `LayerMeta` を丸ごと差し替えるので呼び手が `timing` を黙って戻せる — 同束で見る。なお `masksProperties`(layer-meta 束)は Mask 型の死蔵を避けるため mask 束が先行着地させた |
| 109 | 2026-08-20 | **shape-2/3 の敵対的レビュー結果**(ミューテーションで実証、使い捨て worktree にて): 実装は反証に耐えた — defect#1(join_corner Round)の修正と試験は本物、pucker-bloat の式は lottie-web 固定 commit と一致、gradient の空/1本/半径0 は tiny-skia 実ソースと突き合わせて安全、依存混入なし。**ただし柵の穴4つ**: (a) defect#2(全曲線閉路の offset)の修正に当たる試験が無い — 差し戻しても48本緑のまま (b) polystar の起点固定(真上)が位相反転しても緑 (c) twist の度→ラジアンを消しても緑 (d) polystar pt<3→空パスの境界に試験が無い。**追加試験束で塞ぐ**。付随裁定: gradient stops は**構築時に offset 昇順へソート**する(tiny-skia は自動ソートせずクランプするだけ — 非昇順入力で見た目が未定義になる)。pucker/bloat の極性の**呼び名**(数式でなくラベル)は AE 実機の確認待ち |
| 110 | 2026-08-20 | **pucker/bloat の極性ラベルは正しかった**(裁定109 の保留を閉じる)。独立3系統が一致: lottie-web の実コード(正の percent で頂点が重心へ・ハンドルが外へ)/ AE 英語圏解説(positive=bloat)/ Illustrator 日本語UI(負=パンク・正=膨張)。地図・試験の「正で bloat / 負で pucker」は変更不要。AE 実機確認は不要へ格下げ(孫引きだが3経路の完全一致で足りる) |
| 111 | 2026-08-20 | **marker / split-position / transform-skew の3束が完了**(8/8、evidence 全件がコードに実在)。判断3点: (a) **skew は velato(linebender/velato, `src/runtime/model/animated.rs`)の式をそのまま移植** — `R(-axis) · shear_x(tan(skew)) · R(axis)` を `rotate` と `scale` の間へ挟む(裁定58 の順序の穴を埋めた)。`LayerPlacement::from_transform` の呼び手9箇所(view.rs 1 / r1.rs 2 / compose.rs 6)を機械的に更新した (b) **split-position は `position`(Vec2)優先・無ければ `position.x`/`position.y`(F64)を合成**という2段フォールバックにした。新しい Intent は要らない — 既存の `PropertyId` の平坦な流儀(裁定92)にそのまま乗る (c) **marker は `Intent::SetMarkers` + 新 component**(`Composition:markers`)。`Composition` 構造体に `markers` field を足す案は却下した — 呼び出し側(engine/export/shell 含め14箇所以上)を全部壊す割に、`SetMasks` と同型の別 Intent にする方が変更範囲が狭い。**地図の `composition.animation.markers`(layer-meta 束の行)はまだ 採用予定 のまま** — 中身(component・読み書き口・`flattened()` 経由の永続化)はこの束で実装済みなので、layer-meta 束は状態と evidence を書き換えるだけでよい(裁定108(d) の `masksProperties` と同じ形の先行着地) |
| 112 | 2026-08-20 | **layer-meta 束が完了**(17/17、evidence 全件がコードに実在)。裁定108 の宿題2件を閉じた: (a) **`flattened()` を「component を名前で列挙する」形から「store に聞く」形へ構造修正**(裁定57)。新設 `StoreView::track_json_components` が entity の `TrackJson` 系 component を全部読んで返し、`Document::copy_track_json`(内部は `write` と同じ `ingest` を通る)がそのままコピーする。**柵の試験を先に赤で確認した**(`tests/flatten_fence.rs` — 旧手列挙のままだと `Layer:attrs` を運べず落ちることを実際に確認してから直した) (b) **`Intent::SetMeta` を新規配置専用にした** — 既に `meta` を持つ layer に対しては `write` が `Err` を返す。既存 layer の素材/重ね順を変える口は `SetSource`/`SetOrder`(`SetTiming` と同じ read-modify-write)に分離したので、「meta を触ったら timing が戻る」形は型として作れない。旧来の2回目 `SetMeta`(`motolii-engine/tests/frame.rs`)は `SetSource` へ書き換えた。地図17行の判断: (c) hd/parent/bm/matte/nm/ao は新設 `LayerAttrs`(`Layer:attrs` component、`meta` とは別)にまとめた — masks と同じ理由(別 component なら構造的に `timing` を巻き込めない)。`SetAttrs` は丸ごと差し替えだが、`meta` を一切含まないので安全 (d) **`parent` は循環参照を書き口で拒む**(`validate_no_parent_cycle`)。**親の transform 合成は未実装**(`resolve` はまだ `parent` を読まない、保存/循環防止のみ) (e) **`bm`/matte は `ResolvedLayer` まで届くが、合成器はまだ読んでいない**(`motolii-compositor` の blend 実装待ち) (f) **`ao` は `LayerAttrs` に保存するだけで、`resolve` はまだ適用しない**(向きを合わせる計算は未実装) (g) **`sr` は新設 `Speed`(有理数 num/den)** — `source_frame` の進み幅を `i128`/`div_euclid` でスケールする。f64 も fps も式に出さないので TM-4 の柵(裁定32)の対象外のまま保てる (h) **`tm`(Time Remap)は `property::TIME_REMAP` の値があれば `source_frame` を直接上書き**する専用 property(timing には混ぜない、裁定65 の裏返し) (i) **null/shape-layer/text-layer は `LayerSource` の unit variant**(`Null`/`Shape`/`Text`)にした — `LayerSource` は engine の texture cache キーで `Eq`+`Hash` が要るため、`f64` を含む図形本体やテキスト内容を同居させられない。中身は別 component(`Layer:shapes`/`Layer:text`)に置く (j) **shape-layer の中身は `motolii-vector::Shape` をそのまま**(shape-1/2/3 が既に決めた正本、裁定10)— `serde::Serialize/Deserialize` を追加しただけで語彙は作り直していない。依存は増えない(`serde` は既に workspace 全体に居る、`Cargo.lock` の差分2行で確認済み) (k) **text-layer は素の文字列1本だけ**(`Layer:text`)。範囲スタイル・アニメーター(裁定82/85/92)は `text` 発注単位(75行、未着手)の仕事で、ここでは作っていない (l) **`ef`(Effects)は id+plugin_id の列だけ**(`EffectInstance`)。`en`(Enabled)と param 値は `effect` 発注単位(10行)の仕事のまま — layer-meta の地図の note が「効果の列を持つ形」とだけ言っているのに沿わせた。`owns:` 総行数は 8,536→9,185(+649)で、今回は削れる死蔵ではなく実装した分の増加 |
| 113 | 2026-08-20 | **空間モデルの正本を旧台帳から回収し、利用者が再確認**(2026-07-16 M2 CompCamera 決定・2026-07-23 系譜回収が正本): **世界は1つ・カメラは1つ**。2D は別世界ではなく**同じ XYZ 世界の z=0 に居る**(「enable 3D」モードや 2D/3D スイッチという空間分割を作らない — これが Motolii の中核コンセプト)。カメラは `Composition.camera` 1個で、**カメラレイヤ・レイヤ別カメラ・shot switch は持たない**。第1変種は平面正射影(裁定14 はその実装 = 既定カメラへ再スコープ)、**Spatial/Perspective は additive な将来変種**(向きの表現・handedness・特異点・移行を決めてから)。**パララックスは絶対**(層の z で生む。将来の3Dレイヤー・点群撮影が rerun 選定理由の一つ)。観測用の pan/zoom/fit は Session(裁定46/107 と一致、旧正本も同文)。**カメラに張り付く層(pinned)は明示属性**で持つ — AE の「2Dレイヤはカメラを無視」という空間分割の形は採らない。旧 camera 資産1,256行は回収候補(裁定39) |
| 114 | 2026-08-20 | **ギズモは Ravel から移植する**(スクラッチでも crate 依存でもない)。棚卸しの結果: Ravel `overlay.rs` の `ShellManipulator`(8ハンドル・コーナー外側回転リング・アンカー+親リンク・修飾キー、単体テストつき、Apache-2.0 OR MIT)は意味と数学が toolkit 非依存に切ってあり、GPUI に触る末端だけを iced Canvas へ差し替えればよい。`transform-gizmo` crate は3D(quaternion/view-projection)前提で 2D モデルに不要な抽象を運び、かつ skew が無いので**依存に足さない**。skew ハンドルはどの候補にも無いため唯一の新規(store の `skew`/`skew_axis` は実装済み)。層→画面座標の query を engine に1本足せば shell は投影のみの規律を保てる |
| 115 | 2026-08-20 | **preview は透視カメラの 2.5D にする**(利用者裁定 2026-08-20、「プレビュー時はインパクト重視」)。層は z=0 既定のまま、Stage は `Projection::Perspective` の下で描く — z を動かした瞬間に視差が出る形。**カメラの向きは固定軸のまま**(真っ直ぐ -z を見る): 旧正本が Spatial 変種の前提に挙げた難所(向きの表現・handedness・特異点)は**まだ開けない**。動くのは center / zoom / roll(面内)+ 層の z だけで、これなら透視でも決めるべき姿勢表現が無い。preview = export は同じ関数のまま(裁定15/18 不変)。全 z=0 のとき絵が正射影と一致することは試験で縛る。pinned 層(裁定113)はカメラ変換を受けない。D12 として GOALS に登載 |
| 116 | 2026-08-20 | **camera 束を実装**(裁定113/115 の camera レーン)。`Composition.camera` の center/zoom/roll は新機構を作らず**既存の `PropertyId`/`KeyframeTrack` にそのまま乗せた** — `PropertyId::camera(name)` が `Composition:{name}` という別 component 名前空間を作り(layer 側の `Layer:{name}` と衝突しない)、`Intent::SetCameraTrack` が `/composition` entity へ書く。`LayerPlacement` に `z: f32` を足し(`position.z` track、既定0、split-position と同じ流儀)、`LayerAttrs` に `pinned: bool`(既定false)を足した。**投影の正本は `motolii-core::camera`**(`LayerPlacement::from_transform` と同じ「1箇所だけで組む」形): カメラは常に world +Z を向く固定軸(裁定115)で、`center` はカメラ位置そのものの平行移動(truck — 見る向きは変えない)なので視差が数学的に必然になる。zoom はレンズの画角(vertical_fov)を変えるだけでカメラ位置は動かさない。z が大きいほどカメラから遠い(AE と同じ符号)。**pinned はカメラの逆行列を層の transform に前もって掛けて打ち消す形**で実装し、2パス目を増やしていない(`camera_screen_from_world_z0` が z=0 平面でのカメラの写像がアフィンになることを使う)。**この束固有の自由なデザイン定数**(裁定113/115 はカメラの動く量しか決めておらず、以下は他に根拠が無いのでここで決めた): zoom=1 での垂直画角 55°(`CAMERA_BASE_VERTICAL_FOV_DEGREES`)。**実測**: 全層 z=0・既定カメラの画は旧正射影と**byte 一致(max_diff=0、境界1px 含む全画素)**(`motolii-compositor/tests/camera.rs::default_camera_all_z0_matches_orthographic_pixel_mapping`)。視差・zoom の等方性・roll の向き・pinned 不動は `motolii-core::camera`(GPU 無し、機械精度)と `motolii-compositor`/`motolii-engine`(GPU 込み、画素)の両方の試験で独立に縛った。**残した穴**: (a) 層の z がカメラに近すぎる/背後に回ると near plane を割り込みクリップされる — 裁定に無い自由変数(distance)の帰結で、対症療法はしていない (b) shell/UI にカメラを触る口(gizmo・pane)はまだ無い — `Engine::render_frame` の入口(`view`)がカメラも運ぶので shell 側は無改造で済んでいるが、利用者が center/zoom/roll を書く手段が無い(裁定114 のギズモ移植レーンの続きになる見込み) (c) `Value::Vec2`/`Value::F64` の型検査はあるが、camera 3 property の型不一致時のエラーメッセージは layer property と同水準止まり |
| 117 | 2026-08-20 | **デザイン値は外出し+watch、コードのホットリロードはワークフローにしない**。調査の事実: (a) iced 0.14 に公式ホットリロードが実在(`iced/hot` + cargo-hot、subsecond 系)するが作者自身が「OS ごと落ちうる」成熟度で、`State`/`Message` 変更(widget 新規開発で最頻の変更)で無効 — 試すのは自由、前提にしない (b) hot-lib-reloader 系は iced との未解決クラッシュ実績があり不採用 (c) **iced の Theme/Style は色・境界・影しか持てない(API 実測)— 寸法の Theme 化は不可能**なので既製 crate は全部色止まり。採る形: **自前の Tokens 構造体を State に持ち、debug ビルドのみファイル読込+notify watch(re_renderer 経由で依存グラフに既在)+Subscription で更新、release は const に畳む**。前提工事: 寸法の機械可読正本を1枚新設し Ableton 実測(2026-08-19)を写す — 色 token(motolii-dark.json)と対になる |
| 118 | 2026-08-20 | **layer-meta 束(`0dac5c97`)への敵対的レビューが実証した欠陥をミューテーション検証つきで修正**(全件「落ちるテスト→修正→緑」の順で確認、`next/core/motolii-store/tests/id_allocation.rs` 新設): (a) **id 再利用**(根): 削除は墓標(裁定2/56)なので、shell の `next_layer_id()` が「現存層の最大+1」で採番すると、最大 id の layer を消してすぐ置き直した時に墓標の id を再利用してしまい、その id には既に `meta` component が残っているため `Intent::SetMeta` の新規配置専用の柵(裁定108(c))に引っかかって正当な新規配置が `Err` になっていた(再利用そのものが起きれば、死んだ layer のトラック/マスクが新しい layer に付き直る、もっと悪い事故になる)。**採番の正本を `StoreView::next_layer_id`(墓標込みの最大 id + 1、`sorted_entity_paths()` を tombstone ごと見る)へ集約**し、shell の `next_layer_id()` はそれを呼ぶだけにした(shell の変更はこの1関数のみ) (b) **`apply_all` の部分コミット**: `write` が intent ごとに即 `ingest`(store 追記 + `head`/`tip` 前進)していたため、`apply_all([正当, 不正])` は正当な分だけ確定して `head` も進んだまま `Err` を返していた。**バッチ内の全 intent は同じ edit 刻み `at` へ書き、途中で `Err` が出たらその `at` に書いた分を `drop_time_range` で畳んで `head`/`tip` をバッチ前へ戻す**(この batch は一度も「確定した編集」になっていないので undo 履歴には残らない — undo は時間移動のみという裁定2/47/48 の意味は変わらない) (c) **`SetAttrs` の丸ごと差し替え**: `SetAttrs{ hidden: true, ..Default::default() }` を書くだけで name/blend_mode/auto_orient/parent が黙って既定値へ戻る事故があった — `SetMeta` を新規配置専用へ締めた(裁定108(c))のと同型の事故が `attrs` component の中で1階層下に再生していた。**`Intent::SetAttrs` を丸ごと差し替えから部分更新へ変更**(新設 `LayerAttrsPatch` — 全フィールド `Option`、`None`=無変更)。`write` が現在の `attrs` を読んでから `Some` なフィールドだけ重ねる(`SetTiming`/`SetSource`/`SetOrder` と同じ read-modify-write)。`LayerAttrsPatch::default()` は全フィールド `None` なので `..Default::default()` が型的に安全になる。既存 `tests/layer_meta.rs`/`tests/flatten_fence.rs`/`tests/camera.rs`(store・engine 両方)の呼び出し12箇所を追随、新規試験2本(`patching_one_field_leaves_the_others_alone`/`patching_unrelated_fields_does_not_clear_an_existing_parent`)を足した (d) **`StoreView::track_json_components` の静かな取りこぼし**: `Layer:present` 以外は全部 `TrackJson` のはずという前提を検査しておらず、型が合わない component は `component_batch::<TrackJson>` が `None` を返すのを「値が無い」と同じ扱いで `filter_map` が飲み込んでいた — 将来 `Layer:present` 以外に別 `Loggable` 型の component が増えると `flattened()`/`save()` から気付けず消える。**「値が無い」(`component_batch_raw` も `None`)と「値はあるが `TrackJson` として読めない」(`component_batch_raw` は `Some` なのに型付き読みが `None`)を区別し、後者だけ `Err`**(裁定37 と同じ非同義)。戻り値を `Result` にし `persist.rs::flattened()` の2箇所を `?` へ (e) **負の `Speed` の未クランプ**: `sr`(Time Stretch)が負(逆再生)で `source_in` が小さいと `source_frame` が負値へ落ちる。store は 0 へ丸めない(0 は「素材の先頭」という意味を持つ値なので混同の方が危険)— **doc コメントで明記し特性試験を1本足した**だけで、クランプ/ループ/エラーのどれにするかは engine 側の判断として範囲外のまま残す(黙った穴にはしない) (f) **DRY**: `persist.rs::flattened()` の archetype 文字列("motolii.archetypes.Layer"/"…Composition")のベタ書きを `components.rs` の `archetype_layer()`/`archetype_composition()` へ集約、`descriptor_*` 各関数もそこを参照するよう揃えた。`motolii-store` の試験は 71→79 本(+8: `id_allocation.rs` 新設2本、`document.rs`+2、`layer_meta.rs`+3、`lib.rs` 内部試験+1)、`cargo test --workspace` 赤ゼロ(既知の負荷予算試験2本の flaky さは対象外のまま緩めない)、`check.sh` 全通過 |
| 119 | 2026-08-20 | **グループレイヤーの意味を起草**(理由の全文は `../docs/reviews/2026-08-20-group-layer-semantics-decision.md`)。プリコンポの仕事を明示機構へ分解: 変換=parent(済)/ 整理=グループ(Document)+fold開閉(Session)、伝播は検査系トグル(m/s/l)のみで合成コストゼロ / 群への不透明度・効果=**隔離グループ**(`isolate`、CanvasGroup型のフレーム内部分合成)/ 性能=**フリーズ**(可逆キャッシュ、fingerprint鍵)とフラット化(明示確定)の2段(Ableton Freeze/Flatten踏襲、利用者裁定)。**継承opacityをプロパティごと持たない**(Godot #7293の罠クラスを不存在で消す)。clip×隔離は明示排他。Godot/Blender一次資料で3者同型を確認。実装束は文書を仕様源に |
| 120 | 2026-08-20 | **Timeline pane 第1波が着地**(行・bar・ruler・playhead・マーカー・scrub・選択。M3 の残り半分)+ **デザイン値の外出しが稼働**(裁定117 の実装: 寸法正本 `shell/motolii-shell/tokens/dimensions.json` 新設=Ableton実測値、色は token 正本を直読、debug は notify watch で保存即反映・release は const)。**利用者が実機で初のUX合格**を出した(undo同期を評価)— 旧世界の「良い判定が機械検証止まり」がここで破られた。行順は LayerId 昇順(行位置は packing であって Document の意味ではない — ui-score-model 踏襲) |
| 121 | 2026-08-20 | **text 束の第2切片(range selector とアニメータ)を実装**(25/44 行、evidence 全件がコードに実在。うち constants/text-shape 1行は指示の要約文には無いが `text-range-selector sh` の型に要るため合わせて採った)。`TextRange`(id・name・selector)/`TextRangeSelector`(based_on・range_units・shape・randomize の**静止部分のみ**)/`TextAlignmentOptions` を `TextDocument` に追加。**`text-range a`(Style)と selector の動く部分(Start/End/Offset/Max Amount)はフィールドとして持たない** — マスク/effect と同型で `PropertyId::text_range_*`/`text_range_selector_*`(`text_range.{id}.….` の平坦な名前、`TEXT_RANGE_PREFIX`)経由の普通の `KeyframeTrack` に乗せ、**track の有無が「その animator がその属性を触るか」を表す**(裁定20 の応用)。`text-range-selector rn`(Randomize)は `TextRandomize{ seed }` で **seed 必須**にした(裁定75/101、先例なしで自前設計)。id の重複は `SetTextDocument` の中で `text::validate_unique_ids` が拒む(マスクと同型の柵)。position/scale/rotation/opacity/skew(`text-style` が継承する `helpers/transform`)は Rive `text-modifier-group`(次切片)まで持ち越し、`TextDocument` 拡張は全部 `TrackJson`(serde JSON)経由なので `components.rs`/`persist.rs`/`view.rs` は無改造。`tests/text.rs` は 11→23 本(+12)、`cargo test --workspace` 赤ゼロ(既知の R2 予算試験の flaky さのみ対象外、緩めない)、`check.sh` 全通過 |
| 122 | 2026-08-20 | **text 束の第3切片(Rive 由来の modifier/style span)を実装 — text 束が完了**(残り28行が全部 採用済、evidence 全件がコードに実在。地図全体は `report_work_packages` で `text 残り 0 / 済 75`)。**28行中16行は新しいコードを要らなかった** — `text-modifier-group` の6行(modifyFrom/modifyTo/strength/unitsValue/typeValue/offset)は Lottie `text-range-selector` の `s`/`e`/`a`/`r`/`b`/`o` と**同じ値の Rive 側の呼び名**(裁定90)で第2切片の `text_range_selector_*`/`based_on`/`range_units` をそのまま指し、`text-style`(rive)の6行(fontSize/lineHeight/letterSpacing/fontAssetId/familyName/styleName)は裁定98 が「Lottie の f/s/fc/lh/tr/sc/sw/of はスタイル表の既定行」と読み直した先を指すので第1切片の `TextDocumentStyle` の既存フィールドがそのまま evidence、`text-style-paint`(@class)は `stroke_over_fill` がすでに体現している。**新しく建てたのは3つ**: (a) `text-modifier-group`(8行、グリフの位置・回転・拡大・不透明度)を `PropertyId::text_range_origin`/`position`/`rotation`/`scale`/`opacity`(`text_range.{id}.transform.{attr}`)に実装。`x`/`y`・`scaleX`/`scaleY` は Rive の `"group"` 注記どおり Vec2 1個(裁定61 と衝突しない)、`modifierFlags` は不採用(track の有無自体が有効フラグ、裁定20) (b) `text-value-run`(styleId)を実装するため `TextDocument::style`(単数)を `styles: Vec<TextDocumentStyle>` へ広げ、`TextStyleId`(添字ではなく安定 id、裁定85)と `TextRun{ len, style }`(絶対オフセットではなく長さ、裁定87)を新設。`SetTextDocument` の書き口で新設 `text::validate` が id 重複・参照整合性(存在しない styleId を指す run を拒む)・**隣接同値ランの併合契約**(裁定89 — 連続する2つの run が同じ style を指すことを拒み、併合されていないランを型で作れなくする)を検査する (c) `text-style-axis`/`text-variation-modifier` で可変フォント軸の**2層**を作った — スパン側 `TextStyleAxis{ tag }`(絶対値、`PropertyId::text_style_axis`、裁定92 の唯一の例外)とアニメーター側 `TextVariationAxis{ tag }`(Δ値、`PropertyId::text_range_variation_axis`、裁定76「再シェープする層」)が同じタグでも独立した track を持つことを試験で確認(二重帳簿ではない)。`text-style-feature` は `TextStyleFeature{ tag, value: u32 }` で静止設定のまま(裁定92)。字形ラスタライズはやっていない(意味だけ)。`tests/text.rs` は 23→39 本(+16)、`cargo test --workspace` 赤ゼロ(既知の負荷予算試験の flaky さのみ対象外、緩めない)、`check.sh` 全通過。**地図の text 束(source=lottie/rive 合計75行)がここで閉じた** — 発注単位が空になった最初の巨大束(effect 10 / slot 4 / motion-path 2 の3束・計16行のみが残 採用予定) |
| 123 | 2026-08-20 | **R3 probe(点群)常設** — 利用者実データ 196,133点を point_cloud renderer で撮影、視差を画素差分で実証(D12 初実証)。付随: `motolii-compositor::HeadlessGpu` を公開化(probe が GPU 選定方針を再発明しない)、`image` crate は昇格のみ(グラフ新規ゼロ) |
| 124 | 2026-08-20 | **裁定114 を上書き: ギズモはスクラッチ**(利用者実機裁定 — Ravel は 2D でダサい)。意味の手本=AE、見た目=3D-DCC 文法。plan B=transform-gizmo+自前カメラ行列(旧実働構成の rerun 非依存版)を退避先として維持。第1波は静止描画の視覚受入を先行 |
| 125 | 2026-08-20 | **音声の調達調査 DONE** — Rust界隈→Tracktion/GES/MLT/Ardour→ゲーム/WebAudio/放送の全界隈比較の上で owns ~950行(mix/program/MixProducer)は**意図的自作**(全候補が非公開/不可分結合/別領域。GES はバインディングだけで owns の100倍超)。**ffmpeg amix で混ぜない理由 = 対話性(scrub 即応)と評価経路の一本性**(preview で amix は不可能→経路が2本になる)。「探したが無い」には探索範囲を必ず併記(語彙の壁・界隈の壁の教訓) |
| 126 | 2026-08-20 | **表示忠実度の規律: 評価は常に1本(機械保証)+表示の忠実度低下は必ず表示する**(旧 DRAFT バッジ文法の回収)。iced 0.14 の 2MiB 同期アップロード予算(チラつき真因)対策の preview 縮小は view のみで export 無傷。恒久解=表示サイズ適応 box 縮小+倍率バッジ+100%(1:1)パンモード(タスク#17) |
| 127 | 2026-08-20 | **ui_scale** — 全寸法トークンは 100% 時の値で定義し、単一係数(1% 刻み)を読み出し口1箇所で乗算。罫線のみ物理 1px 床。置き場=Workspace(機構が立つまで dimensions.json 仮置き)。視覚仕様の正本= `reference/mocks/ui-scale-and-z.html`(正典化済み) |
| 128 | 2026-08-20 | **ドック/マルチウィンドウは iced 上流既製(pane_grid+daemon)で行く**方針(spike で実測後に確定)。第2窓=同じ StoreView のもう1つの投影(正本1つの配当)。**パネル配置の置き場=Workspace 永続**(Document でも Session でもない) |
| 129 | 2026-08-20 | **Timeline パネルの意味論を決定**(全文= `../docs/reviews/2026-08-20-timeline-pane-semantics.md`)。不変量筆頭「pane 総高は層数に依存しない」。scroll/zoom/fold 開閉= Session。行ヘッダ列幅は token 化して利用者が確定 |
| 130 | 2026-08-20 | **m/s/l** — `LayerAttrs.solo/locked` 追加。solo=非solo層を描かない(**hidden が勝つ**が、隠れた solo も solo 状態には数える — DAW 流儀)。locked=層を変える全 Intent(**RemoveLayer 含む** — 削除は最も破壊的)を理由つき拒否、**解除だけは常に通す**。unlock+編集の同時 patch は不可(明示2手) |
| 131 | 2026-08-20 | **`Composition.background`(RGBA)は Document の意味** — 書き出しにも乗る(Preview=Export 一本のまま)。既定=不透明黒(旧見た目と画素同一・旧保存後方互換)。実装= engine が pinned・最奥(order=i16::MIN — ユーザー同値は既知の穴)の合成層として消費。キーフレーム化しない(動く背景は層でやる) |
| 132 | 2026-08-20 | **裁定16 を部分的に覆す: alpha は fork 改造ゼロの1行で生きる**(`blend_with_background: Premultiplied`、実測 alpha=128 素通し・空=[0,0,0,0])。「固定していた限界試験が直った日に落ちる」設計が機能した。**残り: export の alpha 付きファイル出力と shell の市松合成は別問題**(未実装) |
| 133 | 2026-08-20 | effect 束の型判断 — `Value::Enum(i64)`/`Value::LayerId(u64)` 追加(Hold のみ)。`EffectInstance.enabled` は静的 bool(無効化しても列から消えない)。`PropertyId::effect_param` は Result(名前が plugin 供給のため) |
| 134 | 2026-08-20 | slot 束の型判断 — `SlotId` は**文字列**(Lottie sid が string。連番 id 方式との意図的な違え)。`PropertySource::{Track,Slot}` は untagged serde で**既存保存データと bit 互換**。`track()` は slot 委譲を None に潰し(生の意味のみ)、区別は `property_source()` |
| 135 | 2026-08-20 | 音声①の判断 — symphonia/rubato は motolii-audio 限定依存(2人目の利用者まで workspace へ上げない)。`AudioOutOfRange` は engine 側所有。`PlaybackCounters::record_block` を ring から分離した pure 関数に(rtrb 導入日は pop 後に1回呼ぶだけ)。store の穴3つ(soundtrack/master gain/音声mute分離)は音声②以降 |
| 136 | 2026-08-21 | **検収規則: write-set 外の横断柵(R2/edit_storm 等の性能予算 probe)は supervisor がマージ検収で必ず単独実行する** — m/s/l の `any_solo` 回帰を probe が捕まえた実例(write-set 分離は衝突を防ぐが横断的性質は守らない)。共有型の変更(Keyframe.spatial 等)後は並列レーンの追随を supervisor が機械修正する |
| 137 | 2026-08-21 | **区切りは面でなく線**(利用者実機裁定 — 「Ableton は線」。ui-visual-language の既存規約「余白でなく罫線と明度差」の実装徹底): 要素の区切りは hairline(#1a1a1a、物理1px)で行い、**面の段差は「地」と「入力の窪み」の2段まで**(帯・箱の塗り分けで区切らない)。文字の階層は**サイズでなく weight(400/600/800)と ink 3段**(primary/secondary/muted)で作る — mock ui-scale-and-z の .glyph/.ident/.sec が実装形。tokens に text weight と ink 段を追加し、全 pane を線の文法へ塗り直す |
| 138 | 2026-08-21 | **レーンビルドの基準線と運用**(web検索→計測の順で確定): 合格構成=**常設 warm worktree(役割別・target温存・使い捨て禁止)+ レーンごとの `-p` 集合固定**(集合が変わると feature unification で28.6s vs 1.45s の再ビルドを踏む — 実測)。実測16〜33s で60秒合格。**魔法フラグは無い**: split-debuginfo=unpacked は macOS 既定・debug=line-tables-only 設定済み・sccache/cranelift/-Zthreads/lld/nextest は律速(リンク)に効かず理由つき却下(出典は調査報告)。**支配コスト= motolii-shell の integration test バイナリ10本の逐次フルリンク(test legの9割)→ 1〜2本のハーネスへ統合する**(次の shell 束)。レーン開始前に常駐プロセス(fileWatcher 等)を確認 — MotoliiRn 汚染と同型の実測2例目 |
| 139 | 2026-08-21 | **線化 pass の受入条件を利用者実機指摘で具体化**(裁定137 の適用先): (1) 現 shell は mock ui-scale-and-z(scale_z_demo)へまだ近づいていない — Ableton は**面の色でなく線で区分け**しており、面色の塗り分けで区切っている残余を全部 hairline へ置換する。(2) **Inspector のパラメータ値セルは箱が小さく文字の内余白が無い** — 出所は実測済み: 行高合わせで `text_input` を `.padding(0.0)` にした際に横の内余白まで消えた(`inspector_pane.rs` の value_cell/name_field)。修正は縦を mock の行高のまま**横だけ内余白を戻す**(`padding([0, x])`)+セル最小幅を mock の列グリッド値に合わせる。oracle は inspector_pixel_fence(±1px)の更新と screenshot 器具 |
| 140 | 2026-08-21 | **Timeline 投影の慢性超過の恒久解 = 解析済み KeyframeTrack の revision 鍵キャッシュを store の読み口の裏に置く**(タスク#19 の裁定化)。真因は計測済み(KNOWN): `track()` コストの97%が serde_json 解析(~88µs/call — 裁定11「track まるごと1 component」の代償が投影側に出た形)。**キャッシュを front に持たせない** — R2 probe の目的が「front がキャッシュを持たざるを得なくなる」ことの防止なので、置き場は単一 writer の裏(store)一択。無効化は revision で機械的に行い、読み口の公開シグネチャと Intent 意味は変えない。予算 4000µs は緩めない。正しさの柵 = キャッシュ経由と素解析の意味等価 property test |
| 141 | 2026-08-21 | **市松は AE 型の透明可視化モードへ**(利用者実機裁定: 「市松模様出てなくね」— トグル ON で市松が見えるのが普通の期待。先例=AE 透明グリッド/Photoshop は背景色を viewer 扱いにする): 市松 ON の間、**プレビュー合成は `Composition.background` を敷かず**(背景 alpha=0 相当で合成)、透明画素に市松を敷く。**Document・undo・書き出しは不変**(viewer 状態のまま、裁定127/128 の置き場)。これで「不透明背景では無反応」という状態自体が消えるため、`checkerboard_invisible_reason` の理由文は撤去(死に chrome 禁止、Q0)。Transparent プリセットは背景を実際に透明で書き出したい人向けとして残す。preview/export 分離規律との関係: 第二 render パスではなく、同一合成器へ「背景を敷かない」入力を渡す可視化モード(export 経路は常に背景込み)と整理する |
| 142 | 2026-08-21 | **トンマナは共通要項として tokens+柵で強制する**(利用者裁定: 「全パネルのルールにして、あとから変更すると全部変わるやつ」): (1) 値の正本は tokens 1箇所(色ロール=motolii-dark.json、type/space scale、weight/ink 段) (2) 文法(hairline 色・太さ、面の2段=地/窪み、余白ロール)も tokens 側が持つ (3) **pane コードへの raw 色値・px 直書きを落ちるテストで禁止**(ui_toolkit_dep_policy と同型の柵 — 「だいたい token 経由」への逆行を機械で塞ぐ。構造による強制>監督)。柵の明示除外 = 物理1px hairline 床(ui_scale 非適用)/ データ由来の色(Timeline クリップ色・シェイプ fill 等、ユーザ作品の内容 — token 化すると「テーマ変更で作品の色が変わる」逆事故になる)/ **製品意味の定数**(Gray18=46 等「値そのものが仕様」— テーマでなく意味。ただし散在禁止・定義点は1箇所)/ iced Theme/Style が寸法を運べない件は widget 側読みで対応(裁定117)。**UI chrome の色は例外なく全部 token**(利用者確認 2026-08-21: 「全ての色」の境界は種類で切る — chrome=全面 / データ色・意味定数=除外)。実装は線化第2弾に同梱 |
| 143 | 2026-08-21 | **Browser 系パネル族の要件骨格**(利用者裁定): (1) パネルは**ウィンドウごとに並列で開ける**こと(multiwindow — タスク#16 の dock/multiwindow spike と接続) (2) 種類は**旧世界を要件源**として4つ: **ブラウザ/エフェクト/クリエイト/パレット** (3) 各パネル共通の chrome: **タグ機能・ディレクトリ表示・サムネ表示の3パターン**。意味起草(タスク#14)はこの骨格から始め、旧 RN 資産(`ui/motolii-rn/src`、rn-browser モック)を一次資料として各パネルの意味(特にクリエイト/パレットが旧世界で何を指すか)を照合してから固定する — ここで発明しない。Q0(死に chrome 禁止)は維持: product 側の対応 API が立つまで出さない |
| 144 | 2026-08-21 | **Timeline 操作文法の正典化**(利用者裁定: 「egui版がほぼ持ってるが、スクラッチなので怖い」): egui 版 `timeline_editor`(9,059行)を操作文法の唯一の担い手にしない。**ジェスチャを含む操作体系の意味論**(ヒット域・修飾キー・ドラッグ閾値・スナップ・カーソル・エッジケース)を、(a) egui 版からの意味抽出 (b) Ravel(Apache-2.0、移植可) (c) NeoUtl(AGPL — **構造と意味のみ、コード不可**) (d) 商用ソフト公式ドキュメント(AE 系中心)から採集し、**コードから独立した単一の文法台帳**へ正典化する。既決の土台は維持: AE 型自由配置・trim family 不採用(2026-08-19)。この台帳が iced Timeline 移植の発注仕様になる。**成果物 = [timeline-grammar.md](reference/timeline-grammar.md)(正典、2026-08-21 起草済み。正/否/候の3値書き分け・文法定数表・未決の穴6件)**。採集一次資料は docs/reviews/2026-08-21-timeline-grammar-surveys/ |
| 145 | 2026-08-21 | **Ravel は全面除外**(利用者裁定: 「motolii より劣っている・ダサい」— 2026-08-20 のギズモ除外を全面へ拡大。参照・移植・協働のいずれの候補にもしない)。裏付け: 調査レーンも当該リポジトリを特定できず RESEARCH_RETURN(GitHub の Ravel 名は全部別物)。**代わりに調査範囲を「最初から揃っている」現代モーションエディタ圏へ拡張**(利用者指示): Lottie 圏の公式エディタ(LottieFiles Creator / Lottie Lab)を最優先 — Motolii は Lottie 軸なので直系の先例。加えて Rive / Cavalry / Jitter / Fable(fable.app)等の timeline 操作文法を広域採集する |
| 146 | 2026-08-21 | **操作文法への利用者裁定3点**: (1) キー群リタイム=**Cmd+ドラッグ**(範囲端の比例伸縮、アクション名 RetimeSelection — 各社バラバラ領域への Motolii 規約) (2) **ショートカット・修飾キーをハードコードしない**: 文法は意味アクション+既定割当を定義し、実装はアクション名→キーの keymap 表を1箇所に持つ(後から自由に再割当可能)。pane コードはアクションだけを見る (3) 文法台帳は**地味な操作(縦スクロール・端パン・カーソル予告・ホイールの奪い合い・ドラッグ中の追随表示)も意味論として持つ** — timeline-grammar.md に §5.5 として追記済み (4) **スクロール方向(natural scrolling 等の上下反転)は文法対象外**: OS 慣習を winit/iced が透過するものをそのまま受け、アプリ内で再反転しない・既定では反転設定も作らない(「慣れの範疇」) |
| 147 | 2026-08-21 | **Timeline の面の分業 — レーンバー新設とクリップ面の名前退避**(利用者実機裁定): (1) **レーンバー(行ヘッダ列)を設ける**: スウォッチ+レイヤー名+M/S/L+開閉。視覚正本は mock ui-scale-and-z の行ヘッダ列。M/S/L は**レーンバーで直接設定できる**(メニュー経由だけにしない) (2) **クリップ面にレイヤー名を描かない**: 現 iced 第1波は名前を bar に直描きしているが、将来の**キーフレームオーバーレイ(操作中の行のキーを bar 最前面へ出す)**と必ず衝突する。名前の住所はレーンバーへ一本化 (3) 文法正典に §1.5「面の構成」として5面(レーンバー/ルーラ/クリップ面/ナビゲータ/スクロールバー)の分業を明記。iced Timeline 第2波の発注仕様に含める |
| 148 | 2026-08-21 | **Timeline の明暗リズム**(利用者裁定): (1) 時間方向 — 区間(秒・拍)ごとに地の明暗を交互に(Ableton 拍グリッド陰影と同型) (2) 行方向 — レーン交互のゼブラ(レーンバーも同リズムを共有) (3) グループ階層のインデント・子行表示は **egui 版のレイヤー構造を参考**にする。裁定137 との両立整理: **区切り=hairline、リズム=地の微差** — リズムを区切りの手段にしない。明度差は tokens 経由(裁定142、トンマナ oracle 管理下)。正典 §1.6 に記載 |
| 149 | 2026-08-21 | **操作文法の逆算対象を「TL 編集を持つソフト全域」へ拡大**(利用者指示): AE ショートカット逆算(R6)に加え、Godot AnimationPlayer(**MIT — コード引用・移植候補指摘まで可**)・Blender(dope sheet/graph/NLA/VSE — GPL につき意味のみ)・Unity Timeline/Animation・Unreal Sequencer・Spine 等の timeline 操作意味論を採集し、正典への網羅性監査に使う。判定4値(既載/抜け/対象外/保留)は R6 と同じ型。先例: 2026-08-20 グループ意味起草が Godot/Blender から罠4件を採った |
| 150 | 2026-08-21 | **操作意味論のスクラッチ最小化 — 借用が既定、発明は例外**(利用者裁定): 操作の意味は既存ソフトで確立したものを既定とし、独自意味論は (a) 先例が無い・各社バラバラ (b) 上位拘束と衝突、の場合に限る(逸脱には理由必須)。**理由: 意図を先例へ委託できる**(公式文書・チュートリアル・ユーザの手癖が仕様の担い手になる)+**移行ユーザの認知段差の最小化**。「wraps>移植>スクラッチ」(2026-08-20)の意味論版。正典の拘束7として明記、逆算監査の「抜け」の既定処置=「AE 系の意味をそのまま採る」。**タイブレーク(利用者裁定・同日追記): 借用先同士が衝突したら使用人口の多い方**(実務上ほぼ AE/Premiere 系。判然としない組だけ裁定へ) |
| 151 | 2026-08-21 | **候・未決も推奨+使用人口の多数決で自走確定してよい**(利用者裁定: 「私の意思はここにはいらなくて、ここはユーザ母数の操作が物を言う世界だから。多数決です」)。Lottie 地図で意味が固まった以上、利用者の意思待ちを外し、supervisor が拘束7+人口ルールで候を確定する。利用者の実窓合否(トンマナ等の見た目)は残るが、操作意味論の確定をブロックしない。一括確定の内訳は正典 §8.2(イージング全 param 開放・Alt=複製/Cmd=スナップ一時トグルへ再割当・レイヤーマーカー採用・菱形の形状コード不採用ほか) |
| 152 | 2026-08-21 | **Codex 解約 — 外部 LLM 発注体制の終了**(利用者裁定): 今後の実装・調査レーンは Claude subagent レーン(常設 worktree 運用)に一本化。理由: パッチワーク思想の委託先は**先例・既存実装**であって別 LLM ではなく、「外部 LLM 同士による答えの会話」という視点は不要という判断。旧 codex 資産(~/Documents/Codex/ 等)は歴史的回収源として残る。外部 LLM dispatch 系の runbook/skill は歴史文書化(削除しない) |
| 153 | 2026-08-21 | **effect 消費の縫い目 = compositor 内の layer 単位オフスクリーンパス**(調査 [effect-seam-survey](../docs/reviews/2026-08-21-effect-seam-survey.md) の推し案を採用): `Compositor::render_with_timing` 内に増設し、engine が `translate_blend_mode` と**同型の store→compositor 語彙変換**を持つ。これで (a) 第二 render パス禁止(裁定15/18/141)と両立 (b) 裁定13(trait まだ作らない — compositor ローカル型で受ける)と両立 (c) compositor の store 非依存を維持。実装は重み均等5切片: S1 store resolve 拡張 ∥ S2 compositor 枠 → S3 engine 変換配線 → S4 Glow shader 移植(`spikes/m5-known-implementation/M5-R0/src/glow.rs` 5パス構成が移植元)→ S5 golden 試験。feedback 系はフレーム間状態を要するため vism 第2号以降へ温存 |
| 154 | 2026-08-21 | **UI 完成 campaign =「普通」の地図方式**(利用者裁定: ペルソナを発明せず、「普通の動画ソフトとは」を深掘りして意味を台帳化 — Lottie 地図(裁定68)の再現)。**「普通」の抽出法**: 語彙源=各製品が公開する有限リスト4種(メニュー木・ショートカット表・Window/パネル一覧・環境設定項目)を機械列挙し、N製品横断で同義項目を束ねて「M/N 製品が一級の入口を持つ」**頻度=普通度**とする(過半数=普通、全数=欠くと購入候補外の級。人口重み=裁定150 併用)。対象製品: AE・Premiere・Resolve・CapCut(+判定材料として FCP 可)。台帳は tsv+判定3値(採用済/採用予定/不採用・理由つき)+**未判定0 が完成条件**+check 機械照合。既存資産(2026-08-19 普通のタイムライン先例調査・R5 の AE 棄却分析・刻まれた文法)は判定側材料として合流 |
| 155 | 2026-08-21 | **「段階が来ていない」という差し戻しを地図の判定に作らない**(利用者裁定: 「カメラビューのフレームなども、まだその段階ではないと差し戻すのではなく全てを実装すべき。プロ用ソフトは意味の無い UI を作らない — 全てに意味がある。全て回収できた暁にようやく動画ソフトの顔になる」)。普通地図(裁定154)の判定は3値のみ: 採用済/採用予定/不採用(拘束・哲学との衝突を理由つき)。**採用予定に「時期尚早」は存在しない** — 順序はあっても却下理由にしない。Q0(死に chrome 禁止)との両立: 見た目だけ先に出すのではなく**意味(機能)ごと実装して出す**。例: Stage のセーフエリア・グリッド・アスペクトガイド等のカメラビュー系 UI も回収対象(カメラレイヤーの設計議題=観測/レンダリング分離とは別軸で、UI 表面は地図が回収する) |
| 156 | 2026-08-21 | **カメラレイヤーを議論枠から通常回収へ**(利用者委任: 「観測/レンダリング分離ももう論点ではなく、既知のもので意味を組めるし実装もできる」)。先例の常識として組む: **観測カメラ=作業視点(Document に乗らない・Unity Scene ビュー/Blender viewport/Rerun viewer カメラ相当)/ レンダリングカメラ=書き出し枠(Document のカメラレイヤー・Unity Game ビュー/Blender render camera/AE Active Camera 相当)**。既決との接続: 裁定113(世界1つ・カメラ1つ)の「カメラ1つ」はレンダリング側の話で観測カメラは view 層 — 分離はこれと矛盾しない。回収の型= effect と同じ(縫い目調査 → 意味裁定 → 均等切片)。関連資産: store の camera track・Stage 正対/camera seat(2026-08-18)・Rerun カメラ注入 wrapper |
| 157 | 2026-08-21 | **観測カメラの置き場= Shell 直下の view 専用フィールド**(縫い目調査 [camera-seam-survey](../docs/reviews/2026-08-21-camera-seam-survey.md) の推しを採用 — 市松トグルと同格の precedent)。レンダリングカメラは実装済み(`Composition.camera` 経路)で、**export は `Engine::render_frame(view,t)` 2引数固定の唯一経路により観測カメラから構造的に隔離済み**(呼び手2箇所を grep 確認)。実装4切片: S0 engine の観測視点付き第二エントリ(裁定141型の入力差分・export 経路不変)∥ S1 Shell 状態+トグル → S2 refresh_frame 配線 → S3 Stage overlay(フレーム枠・グリッド — `camera_screen_from_world_z0` 流用)。S1〜S3 は同一ファイルのため直列(T4 着地後) |
| 158 | 2026-08-21 | **普通地図 完成 — 未判定0**(裁定154 の完成条件到達): 1,551行の全判定=**採用済92/採用予定1,195/不採用264**(理由・裁定引用つき、正本 `reference/normal-map.tsv`)。**UI 完成の残作業キュー=採用予定×freq 降順**(最優先42件= freq≥2: Copy/Paste/New Project/Split/Play-Pause/In-Out マーカー族等)。不採用の型: trim family・Fusion ノード・グレーディングスイート級・プリコンポ・VST・立体視・素材新規生成AI・リアルタイム共同編集(single-writer 衝突)。以後の UI レーンはこの台帳の行 id を発注書へ書き、着地時に verdict を採用済へ更新する(Lottie 地図と同じ運転) |
| 159 | 2026-08-21 | **shell の pane crate 分割で並列の天井を上げる**(利用者指摘: 「毎回ウィンドウを丸ごと起動していることが問題 — 分割できればより早い」): 現状は単一 crate `motolii-shell` が全レーンのビルド税と `-p` 粒度の天井。pane(timeline/inspector/settings/stage/lane_bar 等)を crate へ割り、**レーンは自分の pane crate だけを `-p` に買う**。結合点= `Message` enum と `Shell::update` は「pane ローカル message → shell が畳む」の iced 標準型で解く。順序は切片0の教訓どおり**分割を先に済ませてから機能レーンを大量並列化**。手順: 縫い目調査(依存の実測)→ 分割切片(挙動ゼロ変更・テスト集合一致検収)→ 消化フェーズの並列本格化 |
| 160 | 2026-08-21 | **pane crate 分割の実施計画採択**(調査 [pane-split-survey](../docs/reviews/2026-08-21-pane-split-survey.md)): 10 crate = leaf 6(tokens/metrics/fixture/clipboard/state/chrome)+ pane 4(timeline/inspector/settings/stage)+ assembler(motolii-shell 本体に screenshot と統合テスト残留)。依存順: 切片1 tokens(全前提)→ 切片6 state(Session/KeySelector 移設で型循環解消)・切片5 chrome(スタイル吸い上げ)→ **切片7〜10 の4 pane 抽出は互いに独立=並列**。timeline 抽出のみ高難度(状態機械978行の主部+Message wrap 判断)。各切片の検収=挙動ゼロ変更・テスト集合一致(切片0と同じ型)+分割完了時に workspace 全緑と screenshot バイト一致 |
| 178 | 2026-08-22 | **デザイン外出しの徹底**(利用者裁定: 「モックの形を維持できない」問題への処置 — 「デザインを外だしすべき」): pane の寸法・比率は `tokens/dimensions.json` を**必須経由**とし、Rust 側の寸法直書きを禁止する。仕組み自体は既存(motolii-tokens-rs = parse・watch・ui_scale)で、欠陥の実体は **browser 系キーが 0 件**= B3 が外の家に住まず直書きしたこと(タブ帯欠落・比率自前宣言の共通根)。走行中 BR-tabs / PH-handle 両レーンへ差し込み済み(browser 節 / pane_header 節としてキー追加・`_note_*` に導出根拠)。後続: 直書き禁止の機械柵(トンマナ柵=裁定142 の寸法版)。**採番注記: 161〜177 は各 review 文書に実在・本台帳へ未転記(バックログ回収は別途)** |
| 179 | 2026-08-22 | **枠の文法(chrome grammar)**(利用者「野暮ったい」の機序特定 → 方向承認): ableton/figma/AE 共通の4本(同族色・明度1段区切り/単色既定・アクセント=状態の瞬間のみ/枠 ink は内容より薄く/箱と線を減らし affordance は hover)+第5「**箱は状態の器**」= 常時輪郭の原則廃止。仮説2本(アクセント過多・スクラッチ起因)は画素と grep で棄却 — 真因は箱文法。監査台帳 D1〜D6 = [chrome-grammar-audit](../docs/reviews/2026-08-22-chrome-grammar-audit.md)。実装は線化 pass(裁定137)続編・style 層のみ |
| 180 | 2026-08-22 | **transport は timeline pane へ統合**(利用者裁定: 「タイムラインに再生ボタンが無いのは謎。仮で目をつぶってた部分を統合」): 下部 Play バー撤去・timeline pane に transport 帯(map 1041〜1045 の顔+1138 Timecode を採用済へ)。意味は λ 実装済みで UI の顔だけ新設。モック沈黙 → S 文法導出1案+朝の合否の型。TL-transport レーン発注 |
| 181 | 2026-08-22 | **メニューバー= iced_aw menu の fork 移植**(利用者裁定: 「使えないなら使えるようにすべき。ヘッダー部分が欲しいだけ」— MB-2 案A を上書き): iced_aw rev 924be285(MIT)の menu モジュールのみ `motolii-menubar` へ vendoring し E0308×2(fork の Shell::local 新形)を追随修正。wraps>移植>スクラッチの適用。全 crate 採用ではない・fork rev bump しない・上流追随したら vendored を捨てる。menubar レーン発注 |
| 182 | 2026-08-22 | **Settings はポップアップ**(利用者裁定 — intent-bundles **B12 の家「専用 Preferences ウィンドウ」と一致**): 第1段= モーダル overlay(iced 標準 `stack`/`opaque`・即実装可)/ 第2段= OS 窓(multiwindow probe 後 — pane_grid レーンの daemon 化引き渡しメモ参照)。常設 pane からの退去 |
| 183 | 2026-08-22 | **レイアウトは taffy 複合案を採択**(利用者裁定「taffy で概ね問題は無い。blitz で彼らの力は証明済み」— 調査= weblike-layout-survey の案A+C): (A) taffy 0.13 駆動 flex/grid container widget を fork 非改変で自作(probe check 緑実測済み)(B) 検証層= 旧 workspace の blitz 資産を移植 — `css_metrics`(466行・blitz-dom+stylo の計算値抽出器具)が中核、egui 時代の「HTML でパーツ→機械で測って転写」パターン(inspector_blitz/timeline_blitz/blitz_css 計2,849行)の再利用。モックと実装が同じ taffy で矩形を解くため ±1px oracle が taffy対taffy で成立。Blitz の pane 直描画は不採用維持(2026-08-19 観測)。canvas 内部は従来の比率文法のまま。**利用者補足(採択の歴史的根拠)**: 旧世界で blitz 経路を撤廃したのは「当時トンマナが無く値がガタガタだった」から — 今はトンマナ3層が立ち、**数値自体は意味を持たず比率と余白の間隔が意味を持つ**文法が確立しているため、同じ機構が今度は成立する(撤廃も再採用もどちらも正しかった) |
| 184 | 2026-08-22 | **レイヤーとして現れる物は Inspector が家**(利用者裁定 — 動線の一点性: 選択→調整の視線往復を1本に固定。裁定177 の空間版・AE の Character/Paragraph/Audio 別窓散在への反論): 判定式=「対象が選択中レイヤーなら Inspector / プロジェクト・出力・環境・画面なら否」。適用= B46テキスト・B42音声・B04字幕・B16解析・B38エフェクト編集・B02マスク・B03ラベル色・B07プリセットが Inspector へ(キャンバス/右クリック/メニューは入口)。Inspector は型別 section で拡張(別窓化しない)。適用表と残未決6件= [inspector-extension-scope](../docs/reviews/2026-08-22-inspector-extension-scope.md)(赤入れ待ち) |
| 185 | 2026-08-22 | **説明文 UI は下部バーへ(Blender 型)**(利用者裁定「文字で説明するのはクールではない」): 文脈ヒント(今できる操作・修飾キー)と直近操作ログは status band に集約し、パネル内へ説明文をベタ置きしない。既存の「drag to scrub · click to type · Esc to cancel」行(Inspector)が第1号の移設対象。band の左=文脈ヒント/右=状態、の割りは実装時に S 文法から導出 |
| 186 | 2026-08-22 | **アイコンは SVG(Material Symbols 採用可)**(利用者裁定 — transport の文字グリフ「◀▸」が「かなり無理やりで痛ましかった」): 文字を偽アイコンに使う手法を廃止し、iced 標準 svg widget(未使用在庫)+Material Symbols(Apache 2.0・出典明記で vendoring)で置換。tint は tokens 経由。第1弾= transport 5ボタン。以後の細かい UI 改善は SVG アイコンを常用 |
| 187 | 2026-08-22 | **文字非依存の意味伝達(icon-first)**(利用者裁定 — 裁定186 の格上げ): UI の動詞・状態は「文字を読まなくても分かる」を既定にする — SVG アイコン+形+tokens 色が一次伝達、文字は名前・数値・メニュー項目に限る。単語ラベルが要る場面は tooltip(未使用在庫)で添える(icon+tooltip が標準ペア)。動機= 英語規定の translation 膨張予防+視覚重みの整流。iced 標準 widget の「使用感がいい物」(toggler/pick_list/tooltip 等・在庫調査 TOP6)を文字ボタンの代わりに積極採用。適用は新規・改修レーンの受入条件に順次組込(一括置換 campaign はしない — 触った面から) |
| 188 | 2026-08-22 | **窓の浮かし(floating window)を能力として持つ**(利用者裁定「ドッキングだけでなく浮かしも欲しい — Settings パネルはだいたいポップアップだから」): 裁定182 の第2段(OS 窓)を昇格し、**Settings が浮かし第1号**。前提= multiwindow probe(iced daemon 化・単一 device 複数 Surface・`Shell::view` の window::Id signature 変更・Stage 単一 surface 前提の検証 — pane_grid レーン引き渡しメモと在庫調査が論点台帳)。probe 黒なら第1段(モーダル overlay)へ後退、緑なら Settings→他パネルの順で浮かし解禁 |
| 189 | 2026-08-22 | **追いつきターンの運転 = 並列実装×静的検査、テストは一括**(利用者裁定「全部実装して最後に一括 cargo が大丈夫なのが Rust と iced の利点。今は意味の開発でなく追いつくための実装 — スコープは map/束で決まっている」): レーンの検収線= `cargo check --tests -p <crate>`(型まで・テストは書くが回さない=落ちるテスト先行は維持、実行だけ後送)。supervisor は波単位で merge を束ね、**一括 cargo test を波末に1回**。赤は fix-forward(main 直 push 文化と同じ)。適用条件= 意味が既決の消化フェーズのみ — 新しい意味論・store/engine 跨りの束は従来の即検収に戻す。効果= レーン毎の cold テスト税(〜10分)が波1回に集約 | 
| 190 | 2026-08-22 | **字形描画ルート= cosmic-text → swash outline → motolii-vector**(probe 緑・裁定: 依存増分ゼロ = iced fork が既に cosmic-text を引く。英字/CJK 両方で輪郭→ラスタライズ→PNG 実証・lh(行送り)±0.01px・tr(トラッキング)±0.05px の数値一致)。**欠落の実体= engine `texture_for` が `LayerSource::Text` を Null/Group と同じ「描かない」枝に入れていた**(実測)。切片: (1)依存昇格+vector 出口(probe ほぼそのまま) (2)engine 統合(texture_for の枝を繋ぐ) (3)Inspector TEXT section(現状 TEXT 束は grep 0件)。gap: rich-text 受け口は実在・未使用、可変フォント軸は wght のみ、縦書き未検証。正本= [text-rendering-route-probe](../docs/reviews/2026-08-22-text-rendering-route-probe.md) |
| 191 | 2026-08-22 | **意味の正本は Lottie 地図・普通地図は UI 動詞の台帳**(利用者裁定「パス編集などの意味は全て Lottie が持っている。今の motolii に意味は無いと思え」): データ意味(パス・シェイプ・mask・変形・keyframe 等)の正本は `next/reference/lottie-coverage.tsv`(557項目・未判定0)であり、**motolii 側で意味を発明しない**。`normal-map.tsv` は「世の中にどの UI 動詞があるか」の台帳であって意味の正本ではない — 普通地図の行から意味を起こすのは誤り(発注の前提ミスとして VEC/STORE2 レーンへ差し込み訂正)。Lottie に無い語彙を足す時は、地図の行 id を示せない限り発明とみなす |
| 192 | 2026-08-22 | **第三の単位=「結線待ち」**(利用者裁定「合格と不合格しかないからダメ、もうひとつの単位がいる」): 二値(採用済/採用予定・緑/赤)では現実が表せない — 並列運転で「**部品は main にあるが利用者の動線に繋がっていない**」状態が大量に生まれた(split/markers/marquee/sheets/layout/create…)。早く採用済へ倒せば嘘(触れない)、待てば進捗が消える。処置: (a) map の verdict に **`結線待ち`** を新設 — 部品が main に実在し、結線(shell/keymap/メニュー)だけが残る行。**採用済= 利用者が到達できる**の意味を厳格化 (b) check.sh は3分類で数える(到達可能/結線待ち/未着手)。結線波が `結線待ち→採用済` を flip する (c) テスト側の同型の穴(裁定189 で「書いたが未実行」= 緑でも赤でもない)も同じ語で扱う — レーン検収は「合格/不合格」でなく「**受理(結線待ち)/合格(到達可能)/不合格**」の3値 |
| 193 | 2026-08-22 | **パネル級の機能は「無い」ではなく vism(拡張)**(利用者裁定「Lumetri とかは vism だと思う。その辺の区別はしておく」): Lumetri/Fusion/Fairlight 級の**スイート機能**は「この製品に無いので不採用」ではなく **`拡張` verdict(vism = プラグインで実現する圏)**。不採用に落としてよいのは (a) 単一 comp 前提と構造的に衝突する物 (b) 別製品の内部機構に依存する物 (c) 意図が既存行と重複する物 のみ。**判定を誤ると「拡張の哲学=自由の保護」(2026-08-20)に反する** — 継ぎ目を塞ぐ方向の verdict を機械的に付けない。当面の運転は「普通の動画ソフトになるまで走る」(利用者裁定)で、拡張圏は継ぎ目だけ残して後回し |
| 194 | 2026-08-22 | **並列実装は速度でなく「意味の相互補完」で欠陥を出す**(利用者観測「今の複数実装はいい例もあった。意味の相互補完により、見えなかったエラーも見えてきた。ロックの不備などがそう」): 同一波の独立レーンが同じ土台を別角度から触ると、単独開発では出ない欠陥が構造的に露出する。この波の実例 — (1) `Icon::Repeat` 二重 vendoring を**型が捕捉**(テキスト merge は綺麗に通った) (2) BL4 が matte 実装中に **straight/premultiplied の二重 alpha** を発見(background_rect の先例から) (3) TL4 が一括 Split で **next_layer_id の id 衝突**を発見(スナップショット読みの構造罠) (4) 複数レーンが独立に locked/frozen 拒否を実装し**ロック判定の不統一**が露出 (5) PH-handle が `is_over_pick_area` で **grip 帯が構造的に死んでいた**ことを発見 (6) STORE2/VEC が**意味の正本の混同**(裁定191)を露出。**帰結: 並列は「速く作る」手段であると同時に「単独では見えない欠陥を出す」検査器**。write-set を互いに素にしつつ**同じ土台を別角度から触らせる**配り方が、速度と発見の両方を最大化する |
| 195 | 2026-08-22 | **S6 併存の機械監査が「入口の穴」を発見**(MB3 レーンの実測): 右クリックの項目正本を作る際に「全項目がメニューバーかショートカットにも存在すること」を機械検査した結果、**単一入口の動詞3群が露出** — (a) Lock/Hide/Solo は rail の glyph ボタンのみ (b) ラベル色は Inspector の色チップのみ (c) Split は Message が shell 未統合。レーンは**右クリックへの追加を拒否**(足せば「右クリックが唯一の入口」になり Ableton 可視性原理に反する)し、報告に留めた — 正しい判断として受理。**処置: 右クリックに足すのではなく、Layer メニュー等へ第二の入口を作る**(S6 は「複数の入口を持て」であって「右クリックを禁じる」ではない)。副産物: **S6 監査は入口設計の抜けを機械的に見つける器具になる** — 以後 UI 発注の受入条件に S6 監査を入れる |
| 196 | 2026-08-22 | **union 自動解決は加算リストにのみ使う**(supervisor の実害から): 並列 merge の衝突を機械的に union(両側を連結)で解決したが、**実装本体の衝突に適用すると重複関数・E0252・括弧欠落を生む**(browser-pane で実際に main が壊れ、後続レーンが発見・修理した)。適用してよいのは **mod 宣言・enum variant・JSON キー・NOTICE 追記のような「順序に意味が無い加算リスト」だけ**。実装本体の衝突は必ず読んで解く(または片側採用+再適用)。**検出**: union 解決後は必ず該当 crate の `cargo check --tests` を通してから次へ進む(この事故は check を待たずに次の merge へ進んだ結果) |
| 197 | 2026-08-22 | **裁定189(テスト後送)の実費が可視化された**(REPAIR レーンの実測): 「書いたが未実行」のテストを初めて走らせたら**実バグ2件**が出た — (a) `repeat(3,64px)` を taffy は1トラックと数えるのに3を期待していた (b) `StoreView::attrs` が未書き込み layer に `Ok(None)` を返す仕様を `Some` と誤解。**どちらもテストの側の誤り**で製品コードは正しかった。帰結: check-only 運転は**テストの正しさを検証しない** — 波の閉包で必ず実行し、そこで出るのは製品バグとは限らずテストバグでもある(帰属を先入観で決めない)。また **union 破損の根因も判明**: diff3 は「同じ末尾構文を持つ独立関数」(例: 2つの別関数が同じ `) -> Result<(), String> {` で終わる)を誤整列するため、機械 union が関数を継ぎ接ぎする |
| 198 | 2026-08-22 | **並列の強みは「割る圧力」— 漏斗を作るのは強みの放棄**(利用者裁定「並列の強みはコードを分けれること。強みを間違えないでください」): supervisor は merge 安全のため「**結線は常に1レーン**」「型別 section は inspector へ集約」という規律を作り、shell 5,616行・inspector 4,907行を生んだ。これは**並列のコスト(衝突)だけ払って強み(構造が割れる圧力)を捨てた**形で、実害として merge 事故2件が出た。**正しい対処は「1本に絞る」ではなく「複数レーンが同時に働けるように割る」**。以後の運転則: (a) ある面に発注が集中したら、それは**割る合図**であって直列化の合図ではない (b) 分割案の第一評価軸は「同時施工可能なレーン数」 (c)「結線は常に1レーン」は shell 分割の完了をもって廃止する目標規律であり、恒久則ではない |
| 199 | 2026-08-22 | **結線レーンの型 — テストでなく「API 分析の根拠」で担保**(利用者裁定「テストもほぼ要らない、繋いだら分かる。適切に API を分析しているかの根拠は欲しい」+「探索の肩代わり」): 意味が既決で**繋ぐだけ**のレーンは (a) **テストは原則不要**(型検査+実際に繋いだ結果で分かる。落ちるテスト先行の義務は意味を作るレーンに限る) (b) 代わりに **RETURN へ「API 分析の根拠」を必須**: 呼ぶ相手の**実シグネチャの引用**・契約の要点(単位・`Option`/`Result` の意味・既定値・エラー時の振る舞い)・**なぜその口を選んだか**(例: 連続評価なら `track()`・単一設定値なら `value_at()`)。実例= AUD2(秒→RationalTime の換算根拠・`eval` の clamp を根拠に `t=ZERO` 読みの安全性を示した) (c) **supervisor が探索を肩代わりする**: 発注書に「読むべきファイル・関数名・既存の呼び出し例」を先に列挙(レーンは cold worktree で毎回コードベースを探索し直し、tool 使用の大半がそこに消えていた実測)。意味を作るレーン(store/engine/eval/vector の語彙追加)は従来どおりテスト必須 |
| 200 | 2026-08-22 | **並列 worktree で stash 禁止**(EXP3 レーンの実害): `.claude/worktrees/` 配下の worktree は `.git` を共有するため **`refs/stash` も共有**される。EXP3 が stash から戻した時、**同時走行中の別レーン(INS-audio)の WIP を取り込み、自分の退避分は消えた**(相手の pop が持って行った)。復旧は手作業の再適用で済んだが、**黙って他レーンの作業を奪う/失う**構造的な罠。代替: worktree ローカルの commit(いつでも捨てられる)・diff をパッチファイルへ書き出す・commit してから check。**発注書の規律に追記し全レーンへ周知**(被害レーンへはバックアップの所在を通報済み) |
| 201 | 2026-08-22 | **並列は「衝突」でなく「取り残し」を生む — 検知にはビルド関門が要る**(shell 分割調査の実測): settings-pane の AUTOSAVE 節(`b358850b`)が着地してから **53 commit・約1.5時間**、shell 側の2箇所(`update_settings` の match・`ViewModel` 構築)が未配線のまま**誰にも気づかれなかった**。`check.sh` は marker と台帳しか見ずビルドを検査しないため無力。**衝突(同じ行を2人が触る)は git が教えるが、取り残し(片方だけ動いて相手が追随しない)は誰も教えない** — これが並列運転に固有の欠陥クラス。処置: (a) 消費点の一括テストは「あると良い」ではなく**この欠陥クラスの唯一の検知器**(裁定189 の関門を軽視しない) (b) 構造的な予防は **pane が自分の update を完全所有する形**へ統一すること(Timeline/Browser は既にその形・Settings/Export だけ取り残されていた非対称が事故の原因) |
| 202 | 2026-08-22 | **監督が律速なら監督を増やす — 総監督/domain 監督の2層**(利用者裁定「レーン数の追加をしてもいい。監督なせいで増やせないなら、総監督として監督を増やせばいい」): 並列の実効天井は CPU でも write-set でもなく **supervisor の直列性**(発注書を書く・RETURN を検分する・衝突を捌く)だった。処置: **domain 監督**を立てる — 1 domain(crate クラスタ)を委譲し、監督が自分でレーンを切り・検分し・**自分のブランチへ統合**し、総監督へ**1本の報告**を返す。総監督の仕事は裁定・優先順位・domain 境界の設定・domain ブランチの統合・利用者との対話に絞る。第1陣= A: Timeline / B: Stage・Viewer / C: Browser・Settings・Export。**規律は監督経由で伝播させる**(189 検収線・191 意味の正本・196 union 限定・199 結線レーンの型・200 stash 禁止)。効果の測り方: 総監督のツール呼び出し1回あたりに進む map 行数 |
| 203 | 2026-08-22 | **並列の障害は「共有される可変資源」に一般化される**(利用者裁定「レジストリロックは消せる。概念の抽象化をした方がいい」): 今日3つの事故が**同じ概念**だった — (a) `refs/stash` 共有 → レーン間で作業が入れ替わり消えた(裁定200) (b) `~/.cargo` パッケージキャッシュのロック → レーンが直列化(16本規模で顕在化) (c) ブランチの基点が古い → 重複実装3件。個別の禁止則(stash 禁止・基点更新を第1手)を積むのは対症療法で、**正しい型は「発注前にレーンが触る資源を全部数え、共有な物を分離するか避ける」**。分離の手段は資源ごとに実在する(git: worktree ローカル commit・cargo: `CARGO_HOME` 分離/`cargo vendor` による source replacement/target 配布・基点: 発注書の第1手として強制)。**「共有資源の棚卸し」を発注テンプレートの先頭に置く**。実測は resource-isolation probe(裁定203 と同日発注) |
| 204 | 2026-08-22 | **cargo のロック直列化は toolchain 直呼びで消える(shim が環境変数を握り潰す)**(RES probe 実測・裁定203 の具体化): 犯人は `$CARGO_HOME/.package-cache` のロック(2レーン同時で `Blocking waiting for file lock on package cache` が再現・lsof で 4〜7 プロセスが保持)。**罠**: このマシンの cargo は asdf の shim 経由で、rust プラグインの `exec-env` が `export CARGO_HOME=$ASDF_INSTALL_PATH` を**毎回無条件に実行**するため、**呼び手が設定した `CARGO_HOME` は黙って上書きされる**(隔離ディレクトリが空のまま、という2通りの実証あり)。**解**: toolchain のバイナリを直接呼んで shim を迂回し、レーンごとに `CARGO_HOME` を分ける → ロック待ち **3/3 → 0/0**。**効かない物**: `--offline`(ネットワークのみ・ロックは同じ 3/3)・`cargo vendor` 単独(共有 `CARGO_HOME` のままだと 3/3。単独実行では待ちが出ないので「効いた」と誤認する罠つき)。vendor は補助として有効(847 crate/901MB/166s・レジストリ全複製 4.25GB より軽い)だが git 依存(rerun fork)の source replacement が未解決。**発注テンプレートへ載せる**(裁定203 の「共有資源の棚卸し」の第1項目) |
| 205 | 2026-08-22 | **「追加する」の家は Browser ただ1つ**(利用者裁定「それらは全部ブラウザ内に格納します。追加するものはブラウザの中に全部入れる」): ペルソナ調査4本が「**意味はあるのに作る動作が無い**」を4件検出した(テキスト/シェイプ/マスク/エフェクト)。その処置として動詞をメニュー・右クリック・パネルへ散らすのではなく、**追加する意図の家を Browser に統一**する(裁定177「1意図=1つの家」の適用)。Browser は既に4タブ(media/effects/create/panels)を持ち**器は完成している** — create タブ= 新規レイヤー(Shape/Solid/Null/**Text**)・effects タブ= エフェクト適用(Glow カードの `creates` を繋ぐ)・media タブ= 素材。**帰結**: (a) 「New Text Layer」等のメニュー項目は作らない (b) 追加系の新しい動詞は必ず Browser のカードとして生える (c) S6(併存)は「Browser が主入口・メニュー/ショートカットは補助」の形で満たす — 逆にしない |
| 206 | 2026-08-22 | **裁定191 が縛るのは「絵の語彙」であって「編集機構」ではない**(P4b が裁定待ちにした緊張の解消): P4b は型付き link の意味出典が Lottie に無い(唯一のプロパティ参照機構 Expression `x` は不採用)ことを自己申告した。**判定基準は「その機構の結果を Lottie へ無損失で書けるか」の一点**とする。link は評価時に通常の値へ解決される — 焼けば `KeyframeTrack` と区別がつかない = **フレームの意味は 100% Lottie で表現可能**。よって link は裁定191 に抵触しない。**一般形**: 裁定191 は「1フレームが何を意味するか」の正本であり、キーフレーム補間 UI・スナップ・複数選択の一括編集・Timeline のスクロールと同じく、**出力が Lottie 表現可能な編集機構は Lottie の語彙に無くても核として実装してよい**。逆に、**出力そのものが Lottie で書けない物**(Expression 文字列をドキュメントへ保存する・Lottie に無い描画プリミティブを足す)は依然として不採用。これは裁定70/72(効果の `ty` 不採用・`v` 採用)と同じ線を一般化した物 |
| 207 | 2026-08-22 | **「量に耐えるか」は台帳に現れない — ワークフロー通しが唯一の検出器**(ペルソナ第2周の実測): P1b が3つの欠陥を発見した — Timeline レーン一覧に縦スクロールが無い(`scrollable` 使用ゼロ・行高20px で歌詞50〜100枚が物理的に見えない)・Inspector が `session.selection`(単数)しか読まず複数選択への一括編集ができない・複製が単一レイヤーのみ。**3件とも `normal-map.tsv` に対応行が無い** — 「Timeline はスクロールできること」と書く製品仕様が存在しないため。台帳は**動詞の在庫**であって**規模の要件**を持たない。**帰結**: (a) 台帳消化率は完成度の指標にならない (b) 規模の欠陥は「1本を実際に作り切る」通しでしか出ない = ペルソナ調査を単発イベントでなく**周回する検出器**として扱う (c) 新機能の受入条件に「100個あっても成立するか」を入れる。裁定201(取り残しはビルド門でしか出ない)の UX 版 |
| 208 | 2026-08-22 | **「無関係な既存の赤」は無主の赤 — 並列の副作用として誰も直さない**(実測): 今日3本の独立したレーンが、それぞれ自分の担当外として**同じ失敗群**(menubar の文脈メニュー位置決め・stage-pane の gizmo 退化スケール・`viewer_bar_fence`)を「自分の diff とは無関係な既存不具合」として報告した。**全員の報告が正しく、全員の担当外で、結果として誰も直さない**。レーンは write-set で切るので、write-set の外にある赤は構造的に無主になる。拾って発注したところ**3件とも本物**だった — (a) `Stack` の既定サイズが `Fit` で1層目の実寸に潰れ、doc が謳う「全面 `Length::Fill` の透明層」が実現できていなかった (b) glam の `inverse()` が自己アサートするため `is_finite()` 後始末ガードに到達できず、**Scale 0 + Anchor ドラッグで落ちる**実害(release では偶然動いていた・[AGENTS.md](../AGENTS.md) へ知見を記載) (c) 柵テストが**モジュール自身の doc 文言のリテラルに誤爆**していた(柵は正しく、緩めずに doc 表記だけ変えて解消)。**運用**: レーンの RETURN に現れた「無関係な既存の赤」は監督が拾って**無主レーンとして発注する**。「無関係」は担当外の意味であって、赤が偽という意味ではない。裁定201(取り残しはビルド門でしか出ない)の姉妹 |
| 209 | 2026-08-22 | **素朴な全文 grep の柵は自分の説明文に誤爆する**(裁定208(c) の一般化): `viewer_bar_fence` は `text.contains("&mut Engine")` で禁止パターンを静的に検出する柵だが、**その禁止を説明している doc コメント自体**が同じリテラルを含むため、実コードに違反がゼロでも赤になっていた。柵を緩めて黙らせるのは最悪手(将来の本物の違反を見逃す)。**採った処置**: 柵ファイルは無改修のまま、doc 側の表記を分割して意味を保ったままリテラル一致を外した。**一般則**: 全文 grep 柵を書く時は「コメント/文字列リテラルを除外する」か「禁止パターンを doc に地の文で書かない」のどちらかを最初から決めておく。柵が鳴ったら**まず実装側の違反を疑い**、誤爆と判定した時だけ表記を動かす — 順序を逆にしない。**追記(同日・INVERSE レーンの実装が出した答え)**: 最も素直な解は「禁止パターンを探す」のをやめて**明示マーカーの存在を要求する**形にすること — `inverse_fence` は `.inverse()` を含む行に対し直前6行以内の `SAFE-INVERSE` マーカーを要求する。マーカーは地の文に自然発生しない一意な token なので誤爆せず、かつ**「なぜ安全か」を書くことを強制する**(禁止 grep には無い効能)。コメント行の除外(`trim_start().starts_with("//")`)は既存 `marquee_fence` の技法を踏襲 |
| 210 | 2026-08-22 | **書き込み時の検証を「厳しくする」変更はコンパイル門に映らない — `cargo test` が要る**(監督の門の張り方の誤り・実測3件): LINK レーンが `Intent::SetMasks` に「新規 id は `mask.{id}.shape` が先に読めること」という**書き込み時拒否**を足した(P3b 壁7 の根治として正しい)。私は裁定189 の運用どおり `cargo check --tests` 緑を確認して main へマージした — **型は何も変わっていないので門は緑**。しかし実際には既存の呼び手が3箇所壊れていた: (a) `inspector-pane/tests/mask_section.rs` 6件 (b) `shell/src/clipboard.rs::LayerSnapshot::instantiate` が `SetMasks` を property ループ(= `mask.{id}.shape` を書く側)より**先**に push していたため**マスク付きレイヤーの複製が全て失敗**(テストでなく製品動作) (c) 同ファイルのユニットテスト。3件とも別レーンが後から偶然踏んで報告した。**規則**: 追いつきターンの検収線 `cargo check --tests`(裁定189)は**型が変わる変更にしか効かない**。「今まで通った書き込みを拒否するようになる」変更は、**その Intent を書く全 crate で `cargo test` を実行するまで検収完了としない**。発注書に「書き込み時拒否を足すか」を明示的に問う項目を置き、足すレーンには**呼び手の棚卸し**(`grep -rn "Intent::該当" next/ --include=*.rs`)を義務づける。裁定201/208 と同じ「並列では門をすり抜けた物を誰も見ない」系の第3形 |
| 211 | 2026-08-22 | **結果で索引するな、経路で索引しろ**(利用者裁定「先に結果だけを抜き出してしまった。ルートを考えるべきだった」— 裁定201/207/208/210 を1本に畳む): 今日の事故は別々ではなく**同じ1つ**だった。(a) 裁定210: `cargo check --tests` 緑という**結果**でマージした — 見るべきは経路で、「書き込み時拒否を足した」なら拒否は呼び手へ**逆向きに伝播する**(型が変わらないので門に映らない・呼び手3箇所が壊れ1つは製品動作) (b) 裁定207: 台帳は**動詞の在庫**(結果の一覧)で、縦スクロールや一括編集のような**経路の性質**を表す行を持てない — だからペルソナが「1本作り切る」を歩いて初めて出た (c) 裁定208: 「無関係な既存の赤」は、レーンを write-set(結果の区画)で切ったせいで**経路が跨いだ先が無主**になる (d) 台帳の元の並べ替え軸 `freq` は「その動詞がどれだけ標準か」という結果の属性で、`depends`/`weight`(経路の属性)を足すまで順序が付けられなかった。**規則**: 判断の前に「この結果はどの経路の上にあるか」「この変更はどの経路を逆流するか」を先に出す。**既存の器具の限界も明記する**: `check.sh` の `depends` 検査は**自己申告の1ホップ辺**しか見ず、行が存在しない欠落(裁定207)は原理的に検出できない — 経路は多ホップの道であってノードの属性ではないから。**この構造は引き継ぎの蒸留(本日の [handoff-distillation-audit](../docs/reviews/2026-08-22-handoff-distillation-audit.md))と同型** — 引き継ぎで落ちるのも結論でなく経路の側 |
| 212 | 2026-08-22 | **「滑らかに落とし込めた」の正体は資料の4条件 — Motolii 自身の該当物は `Intent`**(利用者の問い「lottie の思想をコードに落とし込む時はかなりスムーズに行けた。どのような再現性があるか」): Lottie が特別だったのではなく、**4条件を同時に満たす資料だった**。(1) **在庫が機械で出る** — `lottie.schema.json` から骨を機械生成し、人間は判断だけを書いた(「何を検討すべきか」を人が決めなくてよい) (2) **閉じているので網羅が機械判定** — `lottie_coverage.rs` が双方向(スキーマに在って表に無い/表に在ってスキーマに無い)で落とす (3) **判断が項目ごとに独立** — 効果の `ty` を捨て `v` を採る(裁定70/72)が互いに干渉しないので並列に消化でき詰まらない (4) **主張が grep で裏取りできる** — 採用済の行は `evidence` に実在識別子を持ち試験が確認する = **自己申告が構造的に不可能**。**対して `normal-map.tsv` は4条件を1つも満たさない**(4製品から手で組んだので上流権威が無い)— 実測: 本日 main が 395 コミット進む間、採用済 227 が**1行も動かなかった**。**帰結**: 4条件を人工的に満たせる資料が Motolii 自身にある — **`Intent`(24枝の閉じた列挙・Document への書き口は背骨1でここ1本)**。「Intent の枝に UI からの入口が在るか」を機械判定すれば、本日ペルソナが人手で歩いて見つけた「意味は在るのに触れない」(`SetShapes` が `create_from_card` から呼ばれない・Glow の `creates` 未結線・マスク追加の口なし)は**全て一つの述語に還元される**。粗い実測で `SetCameraTrack`/`SetCameraPropertySlot`/`RemoveAsset` の入口ゼロを1コマンドで検出(カメラをアニメートできない・素材を外せない)。**2つの限界を明記**: (a) **「呼び手が在る」と「入口が在る」は別** — `SetPropertyLink` は2件ヒットするが両方とも複製経路(`split.rs`/`clipboard.rs`)の match 腕で入口ではない。Lottie 側が実在識別子を要求してこの罠を避けたのと同じ精度が要る (b) **到達可能性の類しか捕まらない** — 裁定207 の「量に耐えるか」は Intent 枝に対応せず原理的に映らないので、ペルソナ周回は畳めない。施工は `check.sh`(LEDGER レーンへ発注済み) |
| 213 | 2026-08-23 | **接続子は加算・ゲートはキーフレーム — 型付き port を作らない**(利用者裁定「ここは Ableton リスペクトで、エフェクトの on,off をキーフレームで設定できるようにして解決。そして値は加算で解決する」): 旧世界 vism の外枠は Kit(= Ableton の Rack 相当のグループプリセット)を型付き port + provider→consumer で繋ごうとして肥大し、Host integration 層が作られる前に世界が終わった(`docs/reviews/2026-08-23-vism-decision-recovery.md`)。**vism は縛らないことが目的**なので、型付き接続はそもそも思想と逆を向いていた。**採る形**: (a) **値の合成は加算** — 同じプロパティを複数の modulator が狙っても可換・順序非依存・決定的で、port も型も優先順位解決も要らない(Ableton の変調と同型) (b) **効果の on/off はキーフレーム** — `EffectInstance::enabled` を単なる `bool` フィールドから track へ(Lottie `en` に対応)。**境界は発明せず型から導ける**: `motolii-eval` の `Value` は `Bool`/`Enum`/`LayerId` の3つに「**補間は Hold**」と自分で書いており(「0.5 番目の選択肢は存在しない」)、これは**加算が無意味な型と完全に一致**する → **補間できる型(`F64`/`Vec2`/`Color`/`Path`)は変調でき、Hold の型は変調できない(単一 source が勝つ)**。`Path` は補間と同じく頂点数一致時のみ。**帰結**: Kit は型無しのグループプリセットで足りる(Ableton Rack が freeform でいられるのは線が1種類だから — 型が無いからではない)。旧世界の `ParamDriver`(`docs/plugin-authoring.md`「テクスチャを触るものは Render 系。**値だけなら ParamDriver**」・参照実装 `SineParamDriver`・`crates/motolii-plugin/` に trait/contract/registry/conformance 実装済み)が「値を出すエフェクト」の正体で、新世界の受け口は `EffectInstance`(画→画)と `PropertyLink`(→値)の2つとして本日揃った — **両者とも `plugin_id: String` + 平坦 params で契約が同一**(`PropertyLink` の doc が「`EffectInstance.plugin_id` と全く同じ扱い」と明記)。**Lottie 表現可能性**(裁定206): 加算の結果は焼けば通常トラック・`en` は Lottie が持つ語彙なので、どちらも無損失で書ける |
| 214 | 2026-08-23 | **Inspector に映る物は全て時間軸で評価できる**(利用者裁定「Ableton リスペクトなら、インスペクターに映るもの全てを時間軸で評価できるようにする必要がある。AE はエフェクトのオンオフしかなくて、変だと思った」): AE は**レイヤースイッチ(表示/ロック/ソロ/シャイ/モーションブラー/調整レイヤー)にストップウォッチを付けていない** — 「プロパティ」だけが時間に乗り、「スイッチ」は乗らない、という区別がある。Ableton は逆で、**デバイスの on/off を含む全ツマミがオートメーション可能**。この製品は Ableton 側を採る。**型の上では既に可能**: `Value` の `Bool`/`Enum`/`LayerId` は「補間は Hold」であって**キーフレームは打てる**(裁定213 が確認)— 乗っていないのは track を張っていないからにすぎない。**境界(規則の破綻を防ぐ唯一の線)**: `Value` に `String` が無いため、名前やフォントの path は時間に乗らない。ここは**同一性(identity)と性質(property)の区別**で切る — 名前・id・フォントの実体 path は identity で、時間で変わる物ではない。それ以外の「Inspector に映る物」は**全て track を持つ**。**変調(加算)可否とは別の話**である点に注意(裁定213): 時間に乗ることは全部に要求し、加算されるのは `F64`/`Vec2`/`Color`/`Path` だけ。**これは AE に対する差分であり製品の立ち位置**(可視性原理 S6 の時間軸版 — 見えている物は触れる、の「触れる」に時間を含める) |
