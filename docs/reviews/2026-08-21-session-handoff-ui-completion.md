# セッション引き継ぎ — UI完成campaign始動日(常時更新)

日付: 2026-08-21 / 状態: **運転中 — 落ちたらここから**(最終更新: U2着地・台帳108/1551・分割切片1走行時点。GitHub push 済み・README 刷新済み)
セッション ID: `99f96467-73b7-419f-8bc9-4f7b44f5a5ae`(worktree: session-handoff-day2-e1fbd3)
**走行状態の正本 = [レーンボード](2026-08-21-lane-board.md)**(このファイルは「疑うべきこと」と再開手順を持つ)

## 1. まず疑うべきこと

| # | 事実 | なぜ疑うべきか |
|---|---|---|
| 1 | **実窓の目視合否がまだ一度も無い** | 視覚検収は全部 screenshot 器具(文字を描かない)経由。theme 結線(実窓の地色)以降、実窓は未起動。利用者の実機合否が最終審判(貯め一覧はボード「利用者の目待ち」) |
| 2 | **採用済92 の中に過大判定が混じり得る** | J判定は grep 基準だが、重複39件の衝突11件を「J1優先」で機械解決した。消化レーンが該当行に触れる時は行単位で再確認 |
| 3 | **backend 消費ギャップが本命の残り** | blend mode(Normal以外拒否)・mask(合成器未消費)・matte・parent変換・speed は**型だけ実在**。UI表層より先にこれらを埋めないと「メニューはあるが絵が変わらない」M13違反を量産する |
| 4 | Play/Pause は未実装のまま採用予定に残存 | 実時間再生=音声クロック束(旧 motolii-transport の audio-clock-master が移植元、KNOWN 参照)。U2 は意図的に除外した |
| 5 | Premiere の語彙源は二次資料 | Adobe ドメインが本環境から全面不達(実測)。freq が過小の可能性 — 地図の頻度を絶対視しない |
| 6 | 既知境界(doc 明記済み) | 観測カメラ中は市松プレビュー非合成 / multi-select のハイライト未配線 / effect 複数 pass は連鎖しない(最後勝ち) / fmt ドリフト既存 |

## 2. このセッションでやったこと(要約 — 詳細はボードと裁定139〜159)

- **土台**: 前セッション123コミット着地・agent規則リセット保全・テストバイナリ統合(リンク半減)・r2根治(15530→495µs)・theme結線・トンマナ3層(tokens/文法/柵)完備
- **Timeline 第2波完結**: 分割→レーンバー→clip move/trim→キー行→hit統合→キー時刻編集(リタイム込み)→ライブプレビュー。正典 timeline-grammar.md(逆算監査8本→処置台帳付き)が発注仕様
- **vism 第1号完結**: effect 縫い目5切片(裁定153)→ Glow が AE 型 halo まで(golden 固定)
- **カメラ完結**(裁定156/157): 観測/レンダリング分離・フレーム枠 overlay・export 汚染ゼロの構造柵
- **普通地図完成**(裁定154/155/158): 4製品の語彙源1,551行・未判定0 — **採用済92/採用予定1,195/不採用264**→消化 U1・U2 着地で **108/1,551**
- **市松 AE 型**(裁定141)・外周1px根治・track解析キャッシュ(裁定140)

## 3. 走行中と直近キュー(順序)

1. **分割切片1: tokens crate**(走行中)→ 切片5 chrome+6 state → 4 pane 抽出(並列)→ **検証(テスト集合一致+workspace 全緑+screenshot バイト一致)= 残りゴール**(裁定160 に全計画)
3. 分割後: 採用予定1,195 の本格並列消化(レーン= pane crate 単位、`-p` 数秒級)。優先= freq≥2 の残り+backend 消費ギャップ(blend/mask/speed — effect S1〜S5 と同じ縫い目の型で)
4. 保留中の玉: Split 動詞(SetTiming 土台あり)・Mark In/Out 族・multi-select ハイライト・New Project/Save As(ファイルダイアログ= rfd 依存裁定が要る)・音声クロック束・キーのコピペ

## 4. 運転規則(このセッションで確立・再確認したもの)

- 常設 worktree: lane-shell / lane-shell2 / lane-engine / lane-store(reset --hard main で追随・target 温存・`-p` 固定)。cargo 並走5本まで・調査は別枠
- **発注は重み均等**(項目別1レーン禁止 — KNOWN レーン運用節。毎回忘れると利用者に指摘された)
- 発注書: cargo 規律を冒頭太字・落ちるテスト先行・map 行 id 記載・着地で verdict 更新・merge は supervisor が main checkout で検収
- 意味論: 借用既定(拘束7)・衝突は使用人口・キーは仮置き(keymap 層未実装)・「時期尚早」却下は禁止(裁定155)
- Codex 解約済み(裁定152)— 全レーン Claude subagent

## 5. supervisor の誤り(このセッション)

- レーンA発注文の作業場所を書き損じ(冒頭で lane-engine と誤記→本文で訂正、SendMessage で念押しして事故回避)
- 裁定151 を台帳の誤位置へ挿入(即修正)
- 線化第1弾の capsule で「screenshot 器具が Inspector を描ける」前提を照合せず視覚受入不能に(器具拡張レーンで回収 — capsule の穴の再演)
- 普通地図 J1 の対象指定に「keyframe系」を跨がせて重複判定39件を発生させた(機械解決したが、カテゴリ排他の指定にすべきだった)
- U2 初回発注で対象 crate のフルパスを EXACT TARGET に書かず、レーンが legacy(crates/)へ誤実装 → 棄却・再発注(KNOWN へ規則化: パスで縛る)
