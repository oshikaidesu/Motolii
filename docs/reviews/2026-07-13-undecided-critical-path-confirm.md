# 友人レビュー確認: 未決事項とクリティカルパス(2026-07-13)

友人レビュー(調査→監査→決定パック→発注書の詰まり方)を台帳・Issue・コードで再確認した記録。**案の採択はしない**(決定は人間のみ=#103)。

## 判定表

| 主張 | 判定 | 補足 |
|---|---|---|
| #103の8項目がすべて「決定: 未」 | **正しい** | #103本文で8項目すべて未決(本確認後に9項目目を追加) |
| D2/D3は#103待ちで凍結 | **概ね正しい** | #103以外にもD2は#99、D3は#100と第二凍結点が着手条件 |
| 人間の承認なしには進まない | **正しい** | #103に「案の採択は人間のみ」「OPENのままD2/D3着手禁止」と明記 |
| コマンド粒度/UI編集状態だけ宙に浮く | **正しい** | 監査メモの残件表にはあるが、#103にも専用Issueにも入っていなかった → **#103の9項目目へ追加** |
| B⑤のコード確認が未了 | **文書状態として正しかった** | 本確認でコード確認まで完了(下記) |
| 波形ピークの記述が残っている | **正しい** | 「低優先化・M3/U3で決めてよい」まで決まっているので、未決ブロッカー扱いだけ外す |
| #56/#57は依存ゼロで並走可能 | **正しい** | 両Issueに明記 |
| プラグインUI関連が未マージ | **正しい** | PR #116と#118がOPEN(放置可) |

## 表現の補正(確定文書向け)

「散逸している穴はもう無い」は、今回確認した台帳・Issue間では支持できる。ただし網羅性を数学的に証明したわけではないので、確認時点の堅い表現は次だった:

> 現在把握している未決事項はすべて追跡先を持つ。ただし「コマンド粒度とUI編集状態の責務分担」のみ専用の決定先がなく、#103への追加またはD2発注書の着手前決定として予約が必要。

**(本確認の処置)**: 上記「コマンド粒度…」を[#103](https://github.com/oshikaidesu/Motolii/issues/103)の9項目目として追加し、宙に浮いた状態を解消する。処置後の堅い表現:

> 現在把握している未決事項はすべて追跡先を持つ(#103の9項目・各D1フォロー・M3/U3低優先など)。網羅性の数学的証明ではない。

## クリティカルパス(厳密版)

1. #103の項目を承認(本確認時点で9項目)
2. D2向けにコマンド粒度/UI状態責務を決定(9項目目。#103内で承認)
3. #99 → D2(#109)
4. #99 → #100 → 第二凍結点 → D3(#110)

**重要な補正**: 「#103だけ承認すればD2/D3が即開始できるわけではない」。D2は#99、D3は#100+第二凍結点も着手条件。

## B⑤ コード確認結果

`crates/motolii-doc/src/schema.rs` の `BlendMode` は通常の serde enum:

```rust
#[derive(Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum BlendMode {
    Normal,
    Add,
    Multiply,
}
```

- 未知値(例 `"blend": "screen"`)は **Deserializeエラー**になる(友人の「現serdeはエラー」は正しい)
- ただし **F-9違反とまでは断定できない**。F-9の未知フィールド保持は主にプラグイン拡張点の契約であり、コア閉集合enumの未知variantにもフォールバックを要求するかは設計判断
- B⑤で決めるべき選択肢:
  1. 未知BlendModeを**拒否**する。ただし「F-9の対象外」と明文化する
  2. `Unknown(String)`相当で保持し、描画時だけNormalへフォールバックする
- **避けるべき**: 単に serde default で Normal へ落とす(再保存で元の値を失う)

## 波形ピーク

[spec-holes](2026-07-12-d1-spec-holes-prior-art.md)残件表どおり低優先化済み。M2仕様の未決事項節は「M3/U3で決めてよい・M2ブロッカーではない」へ1行更新する(本PR)。

## 結論

友人レビューは実務上ほぼ正解。唯一の重要な補正は「#103だけ承認すればD2/D3が即開始できるわけではない」こと。B⑤は「未知variantは実際にエラー」まで確定。ボトルネックは「知らない穴」ではなく**人間の決定待ち**。

## GitHub Issue更新案(エージェント権限不足のため本文は未反映)

本環境のGitHubトークンはIssue編集不可(`Resource not accessible by integration`)。人間または権限のある側で以下を反映する。

### #103 差し替え本文(コピペ用)

```markdown
出典: [spec-holes A4〜A8/B群](docs/reviews/2026-07-12-d1-spec-holes-prior-art.md) / [第二監査 B④改訂](docs/reviews/2026-07-12-code-audit-2nd-d1.md) / 追跡 #97 / [友人レビュー確認](docs/reviews/2026-07-13-undecided-critical-path-confirm.md)

## なぜ決定イシューか

これらをD2/D3の**実装者に決めさせると再び後戻り**になる。Composerへの発注は**決定後の実装のみ**。本issueがOPENのままD2/D3を着手しない。

**着手条件の注意(2026-07-13補正)**: 本issueの承認は必要だが**十分条件ではない**。D2は加えて #99(D1i-1)、D3は加えて #100(D1i-2)+第二凍結点。

## 決定が必要な項目(各項目に案を書いて承認欄を残す)

### A4 同一トラック内のクリップ重なり
- 候補: (1) validateで禁止 (2) 許して描画順を定義
- 影響: D1bフォロー + D3合成順
- **決定:** _未_
- **承認者/日付:** _

### A6 拍0のアンカーと拍子
- 拍グリッド原点(曲開始オフセット由来 vs 独立フィールド)と拍子(小節線)の席
- LFOのbpm同期の位相アンカー前提
- **決定:** _未_
- **承認者/日付:** _

### A8 Effect/Keyframeの安定ID
- M2E-15はLayerIdのみ。位置参照だとreorderでジャーナルが壊れる
- D2のコマンドアドレッシング前提 + 複製時ID再写像規則とセット
- **決定:** _未_
- **承認者/日付:** _

### B① 同時刻キーフレーム
- 拒否 / 後勝ち / ソート済み不変条件
- **決定:** _未_

### B④ hidden/solo/lock(監査で表化必須)
フラグごとに **描画 / 評価 / 書き出し** の3軸表:
- 特に「visible ≠ evaluable」(hiddenでもparent/mask/LookAt対象として評価し得る — Lottie先例)を明示
- **決定:** _未_ (表を仕様へ焼く)

### B⑤ 未知BlendMode
- **コード確認(2026-07-13完了)**: 通常のserde enum(`Normal`/`Add`/`Multiply`)。未知値(例 `"screen"`)はDeserializeエラー
- F-9違反とは**未断定**(F-9は主にプラグイン拡張点。コア閉集合enumへの要求かは設計判断)
- 候補: (1) 拒否し「F-9の対象外」と明文化 (2) `Unknown(String)`相当で保持し描画時だけNormalへフォールバック
- **避ける**: serde defaultでNormalへ落とす(再保存で元値が消える)
- **決定:** _未_

### B⑦ Composition fpsの役割
- スナップ用か書き出し既定か
- **決定:** _未_

### B⑧ 非連続型(bool/enum/AssetRef)のHold規則
- 現`lerp`フォールバックの追認か明示Hold固定か
- **決定:** _未_

### ⑨ コマンド粒度とUI編集状態の責務分担
- プロパティ単位 vs 操作単位、およびUI側(M3)の編集状態との境界
- D2発注書起草の前提。これまで専用決定先が無かった唯一の大物未決(M2仕様の未決事項に残存していた)
- M3ゲートのU2ブロッカー
- **決定:** _未_
- **承認者/日付:** _

## 完了条件

- 上記すべてにユーザー承認の決定文が入り、M2仕様または発注書へ1文以上で焼かれている
- D1hが触るvalidate(A4等)は**決定後にのみ**コードへ反映

## Composer指示

- **本issueで実装コードを出さない**
- 先例調査メモの追記は可。案の採択は人間のみ
- OPENのままD2(#109)/D3(#110)着手禁止
```

### #97 / #109 / #110 コメント要点

- クリティカルパス: `#103承認 → (#99→D2) / (#99→#100→第二凍結点→D3)`
- `#103` 単独では不十分
