# M2基盤再締結ゲート 反対側レビュー（2026-07-15）

対象: PR #192。Composer 2.5とGrok 4.5 Fastへ同一の読み取り専用課題を発注し、主担当が`origin/main`との差分と未mergeブランチを再照合した。

## 判定

初版は**merge不可**。改訂と再レビューを重ね、最終版は**発効宣言PRとしてP0/P1=0、merge可**と判定した。M2再締結完了やM3製品実装の開始承認には使わない。

## 採用した指摘

| 重要度 | 指摘 | 処置 |
|---|---|---|
| P1 | 発効PRと再締結PRの役割が曖昧 | 二段階を明文化し、退出は別PRの証跡表全緑に限定 |
| P1 | D5の依存先の記述がM3粗案と同期していない | U1へ直接、U5へ間接依存と訂正 |
| P1 | #176相当の未merge文書群が棚卸し対象として具体化されていない | 初期集合へ追加し、一括採用禁止を明記 |
| P1 | D1l/D3e、未知plugin→export拒否、journalの審判が粗い | 個別の意味審判と必須証跡面を追加 |
| P1 | レビュー独立性と証跡形式が曖昧 | 実装作者と主担当を分離し、SHA/PR/test/run URLを必須化 |
| P1 | M3停止範囲がタスクとして曖昧 | 新規U0/U1等を禁止し、既にmain到達済みの#180/#191だけを許可 |
| P1 | mainの実装準備台帳がU1f/U2fをREADYのまま残す | M3行をBLOCKEDへ変更し、既存Issueも着手不可と明記 |
| P1 | #176由来の未翻訳IDと棚卸し単位が曖昧 | 影響ファイルの初期集合を列挙し、台帳上のIDも再翻訳待ちへ変更 |

## 採用しなかった指摘

| 指摘 | 判断根拠 |
|---|---|
| CompCamera、Param Pipeline等はmainで既決なので、そのまま再締結閉集合へ入れるべき | レビュアーが`origin/main`ではなく未mergeブランチ`agent/generative-user-boundary`（PR #176相当）の文書を正本と誤認していた。`origin/main`のM2正本はCompositionにCompCameraを含めず、D1j/D1kもタスク表にない。未merge案は採否decisionを先にする |
| mainではGAP-14が未決でD1lは禁止 | `origin/main`にはGAP-14 lifecycle決定、D1l READY、D3e WAITが既に存在する。別ブランチとの比較結果をmainと誤認した指摘であり不採用 |
| U0aだけは許可すべき | `origin/main`のM3正本にU0aタスクはない。既にmainへ到達した#180/#191を保持するだけとし、新しいM3タスクは再締結後に再翻訳する |

## 再レビュー条件

改訂差分について、次を再確認する。

1. `origin/main`と未merge案を混同していない
2. 発効と解除が別PRで、自己充足しない
3. A〜Cの各面に再現可能な証跡形式がある
4. P0/P1が0件である

## 最終再レビュー

対象内容: commit `c1b1106`。`origin/main`を正本、PR #176相当を未merge提案として照合した。

| 回 | Composer 2.5 | Grok 4.5 Fast | 処置 |
|---|---|---|---|
| 初回 | merge反対 | merge反対 | 発効/解除二段化、証跡表、棚卸し、意味審判を追加 |
| 再レビュー | P0=0、P1あり | P0=0、P1あり | 運用正本、M3停止線、D5依存、#176境界を同期 |
| 横断レビュー | **P0=0/P1=0、merge可** | 日常入口にP1=3 | 安全側を採用し、単独再宣言、backlog解禁、CQ-5/U2発注導線を廃止 |
| 最終確認 | 180秒でtimeout | **PASS、P0=0/P1=0** | timeoutを成功扱いせず記録。直前Composer合格後の変更はGrok指摘3点の閉鎖に限定 |

最終的に確認した事項:

1. 発効PRと、A〜C証跡を埋める別の解除PRは循環しない
2. U1〜U8、U1f/U2f/U2g、M3-1、前倒しスパイクを含むM3製品実装に迂回導線がない
3. CompCamera等の#176提案をmain既決と混同せず、一括mergeしない
4. 薄い「再宣言」では退出できず、SHA・PR・テスト名・run URL付き証跡を要求する

判定: **P0=0、P1=0。PR #192は発効宣言としてmerge可**。再締結そのものは未完了であり、D1l/D3e、#176棚卸し、追補実コードレビュー、A〜C証跡を別途完了する。
