# 引き継ぎ(2026-08-23 夕)— 最終チェックに来る利用者へ

利用者の宣言:

> 次に触る時、私は開発者ではなく、**一ユーザーとして批判を行う**。俗に言う最終チェック。

この文書はそのための3点セット。**批判の的を絞るため**に書く — 既に分かっている穴へあなたの目を使わせない。

---

## 0. まず知っておいてほしい1行

**今日の成果は全部「静的検査だけで着地」しており、窓は一度も開けていない。**

裁定219 で `GOALS.md` の状態欄に語彙を入れた:

| 語 | 意味 |
|---|---|
| **済** | **実機で確認した** |
| **結線済(実機未確認)** | 静的には動線から到達する。窓での確認はまだ |
| **結線待ち** | バック実装は在るが到達路が無い |
| **未** | 実装が無い |

**今日書いたものは全部「結線済(実機未確認)」。** あなたが開けた瞬間が、この方法論の実力測定になる。
**私の予想は落ちる。** 落ちた分が次の台帳になる。

---

## 1. 起動(あなたが打つ)

```bash
cargo build --manifest-path "$(git rev-parse --show-toplevel)/next/Cargo.toml" --profile preview -p motolii-shell -j 4
```

```bash
"$(git rev-parse --show-toplevel)/next/target/preview/motolii-shell"
```

fixture(素材を入れずに中身の入った状態で見たい場合)は `--fixture` を付ける。

**私はビルドしていない。** 初回はリンクに数分かかる。

---

## 2. 触ってほしい順序

**台本は `next/reference/procedures/P1-lyric-mv.md`(歌詞動画・145手順)。** そのまま上から辿れる。
今日の実装は P1 と P2 の穴を埋める形で選んだので、**この台本が最短で「今日変わった所」に当たる**。

短縮版(今日いちばん危ない順):

1. **起動 → 素材をドロップ → Timeline に立つ → Stage に絵が出る**
   (M1〜M3。ここが落ちたら以降は無意味なので最初)
2. **Cmd+S で保存 → タイトルの ● が消える → ×ボタンで閉じようとすると確認が出る**
   (今日 C-1 が入れた。4本のレーンが独立に「Cmd+S が無い」と指摘した本丸)
3. **アプリを再起動 → 前回のプロジェクトが開く**
4. **テキストを作る → Content を打つ → サイズを drag で変える → Key を打つ → 別の時刻で値を変える → 再生**
   (今日いちばん深く触った所。A-1b + D-1)
5. **複数レイヤーを選んで削除 / 複製**(C-2。今日まで**複数を消す手段が存在しなかった**)
6. **書き出す → 出来た mp4 に音が入っているか**(C-3。今日まで無音だった)
7. **書き出し中に中断ボタンを押す**(今日まで押せても効かなかった)

---

## 3. 既知の穴 — ここに批判を使わないでほしい

**台帳に載っている = 分かっている。** あなたに拾ってほしいのは**台帳に無い違和感**。

### 構造的に未実装(設計から要る)

- **可搬性**(別マシンで開く・フォント埋め込み・パス解決の見せ方)— P3 後半の17件
- **時間予算**(軽さ)— 計測器具そのものが `next/` に無い。B2/B4/B6 は試験ゼロ
- **キーボード到達性** — iced の `button`/`pick_list` がフォーカスを持たない(上流の穴)。
  裁定217 であなたが優先度を下げた通り、**マウス側を優先している**
- **Zoom to 100%** — viewport bounds が `Program::draw(&self)` でしか取れず `Shell` へ書き戻せない
- **split(Cmd+K)** — 宣言だけで shell へ未統合。GOALS M6 の唯一の残り
- **`WorkspaceBook`**(名前付きレイアウト)— 型は完成、入口なし

### 見た目が追いついていない(機能はする)

- **Key ボタンが状態を表示しない** — 打点済みでも ◇/◆ が変わらない(`projection.rs` が track の有無を持たない)
- **drag 中の値が欄に即時反映されない**(値そのものは正しく書かれる)
- **Composition の W/H/FPS/尺・AutoSave の数値が drag で動かない** — track を持たない値なので既存の drag 機構が使えない。**設計から要る**

### 既知の限度

- 書き出しの cancel は **mux 直前だけわずかに遅れて効く**(`Cancel::is_cancelled` が非 pub)
- `resolve_layer_selection`(Shift/Cmd 範囲選択)は**単体テスト付きで呼び手ゼロ**

---

## 4. 今日何が起きたか(経路。裁定211 に従う)

### (a) 台帳が枝の端しか示せていないことが数字になった

利用者の問い「幹があるはず」から出発し、裁定154 の抽出規則(製品が公開する4リスト)の帰結を実測した:
**1,551行すべてが menu/shortcut/panel/pref のいずれかで、それ以外は0行。**
`marquee`/`hover`/`double-click`/`offline`/`autosave` の grep は全部0件。

→ **幹の軸12本**を導き、並列で掃討 → **穴135件、うち73%(98件)が台帳に名前を持たない**。
手順書3本(347手順)でさらに72件。

### (b) 基準を変えたら測っていた物が消えた

裁定216 が数えた「マウスのみ25件」は、裁定217(マウス完遂を主眼・とんち禁止)の下で**実際の穴 0件**。
実穴は `GESTURES.md` が見ていない**数値欄12箇所**にあった。

### (c) 正典が台帳と同じ病気だった

`normal-map` id1224 Save「採用済」・id504/505「採用済」・id1393「採用予定」・`GOALS.md` M9 cancel「済」。
**4件すべて「バック実装が在る」を「利用者に届く」と取り違えていた。**
→ 裁定219(状態欄の語彙4つ)。

### (d) 責任の集中が数字で出て、分割線になった

軸台帳の `責任` 列が `motolii-shell/src/lib.rs` を **102回**指名(2位の6倍・6,228行)。
**穴の種類がそのまま分割線**になり、11モジュールへ割った(6,228 → 2,127行)。
その結果、波C の4レーンが**互いに素な write-set**で並列できた。

利用者の指摘がここで効いた:

> 事前に並列にできるよう分解して切っておけばコードを最低限にできる。ひとつのファイルの責任は多すぎるとダメ。
> 適当にその場の現場判断で並列を作ったのがダメだった。

---

## 5. 本日の裁定

- **217** A02 はマウス完遂を主眼に。連続量はマウスだけで完遂/文字入力は対象外/**とんち禁止**
- **218** テストは根拠を示してから。書いてよいのは検収条件・回帰の柵・後方互換の3型
- **219** `GOALS.md` の状態欄に語彙4つ。**`済` は実機で確認した物だけ**

---

## 6. 運転の記録 — 私の失敗

1. **レーンを worktree 隔離せずに出した**(11本が main の作業ツリーを共有)。
   損失ゼロだったのは**軸で切って write-set が構造的に互いに素だったから**で、運。以後は必ず指定した
2. **背景待ちの自己停止が13件**。文章での禁止は今日も全敗。復帰の定型文だけが効いた。
   `next/lane-check.sh` は**まだ一度も実行していない**(使う前に必ず自分で動作確認すること)
3. **`cargo test --workspace` を回すまで取り残しに気づかなかった** — `Asset` に `status` が増えて
   browser-pane が壊れたまま main に入っていた(裁定201 の再現)

## 7. 次に触る人へ

- **状態の正本**: `next/reference/axis/BOARD.md`(1枚で現在地)
- **穴の一次記録**: `next/reference/axis/A01〜A12*.tsv`(書き換えてよいのは担当レーンだけ)
- **台本**: `next/reference/procedures/P1〜P3*.md`
- **今日の commit は 91本**。全部 main、push 済み
