# 夜の自走の報告(2026-09-15 夜 → 09-16 朝)

利用者「今から寝るので、全て自走」「3 ばんではなくすべて」。目標は [Sync の画](2026-09-15-sync-anatomy.md) に最短の意図で辿り着くこと、そのための最小コア(レイヤー・箱・時間 + taffy)と、天井を作らない GPU のブロック([口の案](2026-09-15-box-block-interface.md))。新しい意味は全部「提案」。

## できた物

| # | 物 | commit | 確かめ方 |
|---|---|---|---|
| 1 | **GPU のブロックの段**: `vism/` に `"STAGE": "block"` の WGSL を置くと棚に載る。作者は `fn block(k, p) -> Offset` だけ。全員の箱と今のずれ(state)が GPU にあり、効果の順につながる。`"ROUNDS"` で繰り返し、`neighbor(k, i)` で近くの物だけ回る | 39146f7f, 42bc19f7, f9405abe | 試験(4000 箱の Bounce、2000 箱の押し合いが全組と一致) |
| 2 | **読み戻さずに描く**: fork の頂点の段が motion の buffer(物ごとの world のずれ)を読む。rerun fork `03801b1c` | ce9e5f38 | 試験(GPU の Bounce の絵が CPU の Bounce と 1px 以内) |
| 3 | ブロック 3 本: **Bounce**、**Push Apart**(C4D、書類の間合いの押し合いと一致)、**Wave**(Cavalry) | 同上, d5038e2d | 試験 |
| 4 | **付いて置く札が GPU の結果に付いて行く** | b41cde15 | 試験 |
| 5 | **Position Anchor 2**(提案): 2 つの相手の重なり(離れていれば間)に付く。CSS の辺ごとの anchor() | 4777e55c | 試験、Sync の文字が重なった所に付く(跳び 0) |
| 6 | fx **4-Color Gradient**(AE)、**Inner Shadow**(Photoshop・Cavalry) | 619ea3c4, 28d4da7c | 絵で確かめた |
| 7 | **Sync を通しで**: `ui/native/src/editor/script/examples/sync.js`(16 秒、1 本のスクリプト、外の素材なし) | 3bd3ac45 ほか | 動画を送った。コマ間の大きな変化はショットの切り替わりだけ |

## 途中で見つけて直した不具合

- 形の層が **clip の下地になれなかった**(網で描くので黙って落ちていた)→ 下地は絵に描く(9d426145、試験あり)
- fx の欄が 9 個を越えると **束ねが無効で黙って描かれなかった**(device の uniform の上限)→ adapter の範囲へ(28d4da7c)
- device の **storage buffer の上限**が選択の籠の分だけ(1 本・64 KiB)→ 広げた(39146f7f)
- ブロックごとの **欄の buffer を共有していて、後のブロックの欄が前に混ざった**(fa476006、試験あり)
- `field_lens.js` の見本が同梱の見本の試験で落ちていた(私が 09-15 に足した物)→ 見出しを足した

## 天井を外した物([棚卸し](2026-09-15-ceiling-inventory.md))

- 押し合いの全組 → 近くの物だけ(GPU)
- 移り方の配置の解き直し → 書類の版と時刻でコマをまたいで覚える(`stagger_reflow.js` 180 コマの解決 18.3 秒 → 6.3 秒、跳び 0 のまま、7078f984)
- 描き順の番号 `i16`(3 万 2767 で同点)→ `i32`(rerun の並べ替えの鍵へ。深さの補正だけ i16 に丸める)
- 押し合いの升目がブロックの Margin を知らない → ブロックの頭の JSON の `"REACH"` の欄の分だけ升目を広げる

描画の試験: 並列 4 本で 183 通過・3 落ち。落ちた 3 本のうち 2 本は単独で通る(GPU の取り合い)、1 本は前から落ちている radiance

## 窓で確かめてほしい物(窓の再起動は私はしていない)

1. native を build して窓を開き直す(fork の pin が変わった: `03801b1c`)
2. Sync の見本(`sync.js`)を窓で走らせて再生する — 描画・スクラブ・書き出し
3. 棚に Bounce / Push Apart / Wave / 4-Color Gradient / Inner Shadow が並ぶか、Inspector の欄
4. GPU のブロックで動いた物を選んだ時の籠(描いた画素から取るので付いて行く見込み)と、クリックで選べるか
5. 昨日の積み残し: Cmd 吸い付き、Push Trace の棚、Inspector の新しい欄

## 決めてほしい物

- GPU のブロックを書類の CPU の法(Overflow = Bounce、Margin の押し合い)とどう分けるか: 今は両方ある。消すと保存された作品の見え方が変わるので触っていない
- エディタは同じ効果を 1 層に 1 枚まで(Bounce → Push Apart → Bounce が組めない)。ブロックは例外にするか
- 4-Color Gradient の点を % にした(AE は px)
- 物理の身振り(未決の考え)と、3D で引く面

## 次の目標

利用者 2026-09-16「物理の概念があれば解決する。次の目標はそれだ」— CPU の Bounce / Margin と GPU のブロックの二重と、1 層 1 効果の制約を 1 つの物理で畳む。時間の扱い(本物の積み上げか、時刻の純関数の嘘か)を相談中

## まだの物

- 升目が 4×4 から 2×2 へ詰まる動き(Sync 4.5–6.0 秒)、レンズの光(Glow)
- ブロックの 3D(今は comp の平面の軸に沿った箱)、回した親の箱、反射に写る姿のずれ
- radiance の比較の試験は答えのシェーダーが古く、前から落ちている(私の変更ではない)
