# 最優先タスク一覧

作成日: 2026-07-07

`design-memo.md` / `discussion-log-2026-07-06.md` のリスク洗い出しを受け、**実装着手前に決着させないと後続が全て詰まるブロッカー**を優先順に列挙する。すべて現時点では未着手・未決定であり、コードはまだ存在しない。

判定基準:
- **P0 (絶対ブロッカー)**: これが崩れると設計全体が崩れる。他タスクの前提になる。
- **P1 (早期確定必須)**: 着手方向を確定できず、放置すると手戻り・後方互換負債になる。
- **P2 (垂直スライス中に確定)**: MVPを1本通す過程で決めればよい。

---

## P0 — 絶対ブロッカー

### P0-1. WebView UI ↔ ネイティブ(wgpu)描画層の合成方式を技術検証で確定する
- **論点**: 「別サーフェス重ね合わせ(overlay)」か「IPC経由テクスチャ転送」かが未決定(`design-memo.md` 未解決事項)。
- **なぜ最優先**: これがコア設計の土台。overlay はOSごとにz順・リサイズ・DPI・透過・スクロール同期が破綻しやすく、IPC転送は `GPU→CPU→IPC→CPU→GPU` バウンスが入り「軽さ」の目的と矛盾する。ここが崩れると設計全体が崩れる。
- **完了条件(DoD)**: 最小PoCで「動画1フレームをRust/wgpuで描画し、TauriのWebView上の指定領域に正しく表示・リサイズ追従できる」ことを対象OS上で実証。採用方式と実測レイテンシ/コピー回数を記録。

### P0-2. 最小垂直スライス(MVP)の内容を1文で確定する
- **論点**: 「1本の動画を読み込み→色トラッキング→プロシージャルオーバーレイ生成→合成→1フレーム書き出し」で確定するか(`discussion-log` 未決定事項)。
- **なぜ最優先**: スライスが決まらないと P1 以降の技術選定・境界設計の判断基準が定まらない。プラグイン境界の早期凍結(下記の回避対象)を防ぐアンカーになる。
- **DoD**: 入力素材・各ステージの最小仕様・出力形式(単一PNG等)・成功判定を文章で確定し合意する。

---

## P1 — 早期確定必須

### P1-1. トラッキング解析の GPU/CPU 境界を決める
- **論点**: OpenCV.js(WASM) / ネイティブOpenCV(Rustバインディング) / 自前wgpu compute のいずれか。
- **リスク**: ネイティブOpenCVはビルド/リンクが重くクロスプラットフォームで苦労する。`GPUテクスチャ→OpenCV(CPU)→GPU` の往復は 5-2 で禁じた CPUバウンスを解析層で自ら踏むことになる。
- **DoD**: 採用方式と、フレームがどの層(GPU/CPU)を通るかのデータフロー図を確定。

### P1-2. ターゲットOS範囲を決める(Windows優先 vs 最初からクロスプラットフォーム)
- **リスク**: wgpuはバックエンド(Vulkan/Metal/DX12)ごとにバグ挙動が異なり、初期アーキとCI構成が変わる。
- **DoD**: v1のサポートOSと、それに伴うwgpuバックエンドの優先順位を確定。

### P1-3. プラグイン境界/traitの「凍結タイミング」を明文化する
- **論点**: 並列AIエージェント開発のため境界を早期凍結したい(`discussion-log` 未決定事項)が、要件未確定の段階での凍結は後方互換負債を最初に作る。
- **方針(提案)**: 境界凍結は **P0-2 の垂直スライスを1本通した後** に行う、と明示的に位置づける。それまではコア内部の一枚岩実装を許容する。
- **DoD**: 「凍結の前提条件(何が実証できたら凍結してよいか)」をドキュメント化。

---

## P2 — 垂直スライス中に確定

### P2-1. プロジェクトファイル形式の最小スキーマ
- タイムライン/キーフレーム/プラグインインスタンス/パラメータの永続化スキーマ。垂直スライスで扱う最小要素だけを先に定義し、拡張余地(バージョンフィールド)を持たせる。

### P2-2. プレビューと書き出しの一致保証(determinism)方針
- 2パス分離(Remotion方式)で同一結果になる保証手段を設計。GPU描画の非決定性(浮動小数・ドライバ差)への対処方針を決める。

### P2-3. 3D合成と動画合成の検証順序
- どちらを先に垂直スライスへ載せるか(`design-memo.md` 未解決事項)。P0-2の内容確定と連動して決定。

---

## スコープ番人(常時チェック)

- **単独用途(自分のMV制作)** の原則を逸脱していないか。汎用フレーム記述子(YUV数種・premultiplied alpha)やプラグインエコシステム設計が、MVPに対しオーバーエンジニアリングになっていないかを各タスクで都度確認する。
