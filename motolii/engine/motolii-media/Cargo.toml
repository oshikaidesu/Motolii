[package]
name = "motolii-media"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
motolii-core.workspace = true
motolii-store.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
# 点群(`.ply` 等)の import。拡張子の正本は `re_importer`、パーサは
# `re_sdk_types::archetypes::Points3D::from_file_contents`(`point_cloud.rs` 参照)——
# どちらも Rerun 側の実装をそのまま呼ぶだけで、motolii はパーサを書かない。
re_importer.workspace = true
re_sdk_types.workspace = true
re_types_core.workspace = true

[dev-dependencies]
motolii-testkit.workspace = true
tempfile = "3"

[lints]
workspace = true
