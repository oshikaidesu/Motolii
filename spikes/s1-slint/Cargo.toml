[package]
name = "s1-slint"
version = "0.1.0"
edition = "2021"

# ルートworkspaceから隔離(本体はwgpu 30、Slint統合はwgpu 29のため)
[workspace]

[dependencies]
slint = { version = "~1.17", features = ["unstable-wgpu-29"] }
wgpu = "29"
